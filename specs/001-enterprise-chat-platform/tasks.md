---

description: "Task list for Enterprise Internal Chat Platform"
---

# Tasks: Enterprise Internal Chat Platform

**Input**: Design documents from `/specs/001-enterprise-chat-platform/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md),
[data-model.md](./data-model.md), [contracts/](./contracts/)

**Tests**: Tests are MANDATORY per Constitution Principle III (Test-First & Unit Test Coverage,
NON-NEGOTIABLE). Every user story includes test tasks, they are ordered before the implementation
they cover, and they MUST be observed failing first. Integration tests for repository, cache,
messaging, auth, and contract paths run against real PostgreSQL, Redis, RabbitMQ, MinIO, and
Keycloak via Testcontainers — mocking those is prohibited.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1–US9)
- Every task includes an exact file path

## Path Conventions

- **Backend (Clean Architecture)**: `src/InternalChat.Domain/`, `src/InternalChat.Application/`,
  `src/InternalChat.Infrastructure/`, `src/InternalChat.Api/`, `src/InternalChat.Worker/`
- **Frontend**: `src/internalchat-web/src/`
- **Tests**: `tests/Unit/`, `tests/Integration/`, `tests/Contract/`, `tests/Architecture/`,
  `tests/Load/`
- **Deployment**: `deploy/`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Solution skeleton, tooling, and the Docker stack that everything else assumes

- [X] T001 Create solution and directory layout per plan.md in `InternalChat.slnx`
- [X] T002 [P] Create Domain project with zero non-BCL package references in `src/InternalChat.Domain/InternalChat.Domain.csproj`
- [X] T003 [P] Create Application project referencing Domain only in `src/InternalChat.Application/InternalChat.Application.csproj`
- [X] T004 [P] Create Infrastructure project referencing Application in `src/InternalChat.Infrastructure/InternalChat.Infrastructure.csproj`
- [X] T005 [P] Create Api project referencing Application, plus Infrastructure **for DI registration only** — usage outside `Program.cs` is failed by T018 — in `src/InternalChat.Api/InternalChat.Api.csproj`
- [X] T006 [P] Create Worker project referencing Application, plus Infrastructure **for DI registration only** — same constraint as T005 — in `src/InternalChat.Worker/InternalChat.Worker.csproj`
- [X] T007 Configure nullable, `TreatWarningsAsErrors=true`, `AnalysisLevel=latest-recommended`, and central package management in `Directory.Build.props` and `Directory.Packages.props`
- [X] T008 [P] Scaffold React 19 + TypeScript strict + Vite in `src/internalchat-web/`
- [X] T009 [P] Configure ESLint, Prettier, and `tsconfig.json` with `strict: true` and no implicit `any` in `src/internalchat-web/`
- [X] T010 [P] Create test projects in `tests/Unit/`, `tests/Integration/`, `tests/Contract/`, `tests/Architecture/`
- [X] T011 Create the application-host stack with pinned image tags and health checks in `deploy/docker-compose.yml` (postgres, redis, rabbitmq, keycloak, minio, clamav, nginx, api, worker, web)
- [X] T012 [P] Create non-secret placeholders in `deploy/.env.example` and add `deploy/.env` to `.gitignore`
- [X] T013 [P] Create multi-stage non-root Dockerfiles with `HEALTHCHECK` in `deploy/Dockerfile.api`, `deploy/Dockerfile.worker`, `deploy/Dockerfile.web` — **image builds not yet executed; verify with `docker compose build` before relying on them**
- [X] T014 Configure per-layer coverage thresholds (Domain 90 / Application 85 / Infrastructure 60) in `coverlet.runsettings` plus `tools/Check-Coverage.ps1` — coverlet supports only one threshold per run, so the four per-layer floors are enforced by the script; missing data fails rather than passes
- [X] T015 Create the CI pipeline with all 10 constitution gates in `.github/workflows/ci.yml`

**Checkpoint**: Phase 1 complete (T001–T015). Solution builds clean, frontend builds clean,
Compose stack validates, all ten CI gates defined.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Guardrails and cross-cutting infrastructure that MUST exist before any user story

**⚠️ CRITICAL**: No user story work can begin until this phase is complete. The architecture tests
come first deliberately — they are what make Principles I and IV enforceable rather than habitual.

### Architecture guardrails

- [X] T016 [P] Architecture test asserting Domain references nothing outward in `tests/Architecture/DependencyRuleTests.cs`
- [X] T017 [P] Architecture test asserting Application never references Infrastructure in `tests/Architecture/DependencyRuleTests.cs`
- [X] T018 [P] Architecture test asserting Api and Worker name Infrastructure types only in `Program.cs` in `tests/Architecture/CompositionRootTests.cs`
- [X] T019 [P] Architecture test failing the build when any mapped endpoint or hub method lacks an explicit authorization policy in `tests/Architecture/AuthorizationCoverageTests.cs` — **verified by deliberately adding an unprotected endpoint and confirming 2 tests failed**
- [X] T020 [P] Architecture test asserting no Domain entity type appears in an Api contract DTO in `tests/Architecture/BoundaryTests.cs`

### Domain and Application primitives

- [X] T021 [P] Create `Entity`, `ValueObject`, `DomainEvent` base types in `src/InternalChat.Domain/Common/`
- [X] T022 [P] Create `IClock` with a `TimeProvider` adapter in `src/InternalChat.Domain/Common/IClock.cs`
- [X] T023 [P] Define outbound interfaces (`IUnitOfWork`, `ICacheStore`, `IEventPublisher`, `IObjectStore`, `IMeetingTokenIssuer`, `ISearchIndex`, `IPushSender`, `IAuditLog`, plus `IValidator<T>`) in `src/InternalChat.Application/Abstractions/`
- [X] T024 [P] Create validation, logging, transaction, and audit pipeline behaviors in `src/InternalChat.Application/Behaviors/` — hand-rolled dispatcher, **not MediatR** (v13+ is commercially licensed; same Principle VIII trap as MassTransit)

### Persistence

- [X] T025 Create `ChatDbContext` and configuration conventions in `src/InternalChat.Infrastructure/Persistence/ChatDbContext.cs` — snake_case, `timestamptz`, bounded strings, assembly-scanned configurations, plus `ChatDbContextFactory` for design-time tooling
- [X] T026 Create the initial migration with monthly `RANGE` partitioning for `messages` in `src/InternalChat.Infrastructure/Persistence/Migrations/` — extensions plus the three partition-management functions; the `messages` table itself attaches to them in T088, since the entity does not exist until then. **Verified against real PostgreSQL: applied cleanly, partitions create/route/drop correctly, re-apply is a no-op**
- [X] T027 Create the migration runner as a separate idempotent Compose step in `deploy/Dockerfile.migrations` and `deploy/docker-compose.yml` — EF migration bundle, non-root, `restart: 'no'`; `api` and `worker` gated on `service_completed_successfully`. ⚠️ **image not yet built**
- [X] T028 [P] Create an EF Core interceptor that fails integration tests when a request exceeds its query-count budget in `src/InternalChat.Infrastructure/Persistence/QueryCountInterceptor.cs`

### Test infrastructure

- [X] T029 Create the Testcontainers fixture for PostgreSQL, Redis, RabbitMQ, MinIO, and Keycloak in `tests/Integration/Fixtures/StackFixture.cs` — collection fixture, parallel start, image tags pinned to `docker-compose.yml`. **Verified by 7 smoke tests against real containers**
- [X] T030 [P] Create the integration test base with per-test schema reset in `tests/Integration/IntegrationTestBase.cs` — truncate + Redis flush; migration history excluded by case-insensitive prefix
- [X] T031 [P] Create a unit-test guard that fails any test exceeding 100 ms in `tests/Unit/UnitTestBase.cs` — **verified: guard fires over budget, stays quiet under it, idempotent on double dispose**

### Caching

- [X] T032 Implement `ICacheStore` over Redis, rejecting any write without a TTL, in `src/InternalChat.Infrastructure/Caching/RedisCacheStore.cs` — also rejects TTL above a 24h ceiling (catches `FromDays`/`FromSeconds` unit slips); `SCAN`-based prefix invalidation, environment-namespaced keys. Plus `InfrastructureServiceCollectionExtensions.AddInfrastructure()`
- [X] T033 [P] Integration test asserting a TTL-less cache write is refused in `tests/Integration/Caching/RedisCacheStoreTests.cs` — 12 tests against real Redis, covering zero/negative/over-ceiling TTL, prefix invalidation scope, namespacing, and unreadable-entry-as-miss

### Messaging (Principle VI)

- [X] T034 Create `outbox_message` and `processed_message` tables with EF configurations in `src/InternalChat.Infrastructure/Persistence/Configurations/` — partial index on pending rows; composite PK on `processed_message` IS the dedup mechanism. **Verified: both migrations applied to a fresh database, `payload` is `jsonb` with no length cap**
- [X] T035 Implement `IEventPublisher` writing to the outbox inside the ambient transaction in `src/InternalChat.Infrastructure/Messaging/OutboxEventPublisher.cs` — does not touch RabbitMQ and does not call `SaveChanges`; the pipeline's transaction behavior owns the commit
- [X] T036 Declare exchanges, queues, DLX, and capped retry per `contracts/messaging.md` in `src/InternalChat.Infrastructure/Messaging/ChatTopology.cs` — TTL-based retry queues (1s/5s/25s) with a **separate direct requeue exchange** so a retry returns to one queue instead of re-fanning to all; per-queue DLQ so depth alerts identify the consumer
- [X] T037 Implement the outbox dispatcher using `FOR UPDATE SKIP LOCKED` with publisher confirms in `src/InternalChat.Infrastructure/Messaging/OutboxDispatcher.cs` plus `RabbitMqConnectionProvider` — rows marked dispatched only after broker confirmation
- [X] T038 Implement the consumer host with manual ack and `processed_message` idempotency in `src/InternalChat.Infrastructure/Messaging/ConsumerHost.cs` — dedup insert (`ON CONFLICT DO NOTHING`) shares the handler's transaction, so a failed handler rolls back the dedup record; never nacks with requeue
- [X] T039 Integration test asserting a publish failure leaves the outbox row undispatched and redelivers exactly once in `tests/Integration/Messaging/OutboxTests.cs` — 6 tests against real PostgreSQL + RabbitMQ, including rollback-never-publishes and payload-carries-no-message-content
- [X] T040 [P] Integration test asserting a redelivered message is processed exactly once in `tests/Integration/Messaging/ConsumerHostTests.cs` — 3 tests: triple delivery handled once, per-consumer dedup keying, failed handler rolls back the dedup record
- [X] T041 [P] Integration test asserting an exhausted retry lands in the DLQ rather than disappearing in `tests/Integration/Messaging/ConsumerHostTests.cs` — 4 tests: no immediate requeue, TTL return with attempt counter carried, DLQ arrival with failure reason, message never lost. **Suite run 3× consecutively with no flakes**

### API cross-cutting

- [X] T042 Implement the Problem Details (RFC 9457) exception handler with no stack traces or SQL in `src/InternalChat.Api/Middleware/ProblemDetailsHandler.cs` — known exceptions mapped deliberately, everything else collapsed to a bare 500 with a trace id; `UnauthorizedAccessException` returns a **404-shaped** body so a refusal never reveals the resource exists (SC-017). Wired in `Program.cs`. ⚠️ no test yet — contract tests land in T054
- [X] T043 [P] Configure per-user and per-IP rate limit policies for auth, send, search, and upload in `src/InternalChat.Api/RateLimiting/` — partitioned by authenticated subject falling back to IP, so one heavy user cannot exhaust a shared office egress address for the whole floor; auth is IP-only because the caller is by definition not yet authenticated. Limiter shape chosen per surface: fixed window for auth and search, token bucket for send (typing three messages in a row is a legitimate burst), **concurrency** for upload (the cost is a 500 MB transfer held open, not the request rate). Global backstop so a new endpoint is never unlimited by omission; rejections carry `Retry-After` and a Problem Details body. Wired in `Program.cs`
- [X] T044 [P] Configure CSP without `unsafe-inline`/`unsafe-eval`, HSTS, nosniff, Referrer-Policy, frame-deny, and a CORS allow-list in `deploy/nginx/nginx.conf` plus `deploy/nginx/cors.inc` — all headers use `always` so they survive error responses; unlisted CORS origins map to empty, never `*`. **Config validated with `nginx -t`**
- [X] T045 Configure OpenTelemetry traces, metrics, and logs with W3C context propagated across HTTP, SignalR, and RabbitMQ in `src/InternalChat.Api/Program.cs` and `src/InternalChat.Worker/Program.cs` — per-host setup (Api instruments ASP.NET Core, Worker does not), sharing activity-source and meter names via `src/InternalChat.Application/Telemetry/ChatTelemetry.cs`, since Infrastructure creates the messaging spans and Principle I forbids either host naming an Infrastructure type outside `Program.cs`. **Carrying `traceparent` is not the same as joining the trace**: `OutboxDispatcher` now opens a producer span parented to the row's stored context and puts *its own* id on the wire, and `ConsumerHost` opens a consumer span parented to the delivered header. Covered by `tests/Integration/Observability/TraceContinuityTests.cs` — **3 tests, each verified to fail when its parsing is removed.** The first draft of those tests passed against a deliberately broken implementation because `Activity.Current` was still ambient; they now null it out to reproduce the Worker's actual broker-callback conditions. SignalR needs no extra package — hub calls arrive over the negotiated HTTP connection; the hub-method span lands with T097
- [X] T046 [P] Create the observability stack (OTel Collector, Prometheus, Grafana, Loki, Jaeger) in `deploy/docker-compose.observability.yml` — separate file, because losing observability must degrade operability and not the service. Collector is the single egress so swapping a backend touches one config file; it re-strips `message.body`, credentials, and auth headers as defence in depth behind FR-056. Loki retains 30 days, deliberately **not** conflated with the audit log's 1-year requirement, which is PostgreSQL's job. **Merged config validated with `docker compose -f docker-compose.yml -f docker-compose.observability.yml config`**

### Audit (Principle IV)

- [X] T047 Create the `audit_event` table and `IAuditLog` implementation in `src/InternalChat.Infrastructure/Persistence/Audit/AuditLog.cs` — `inet` source address, `jsonb` detail, index behind SC-021; an unparseable address never costs the record
- [X] T048 Grant the application role INSERT-only on `audit_event` in `src/InternalChat.Infrastructure/Persistence/Migrations/` — group role `internalchat_app` with SELECT+INSERT only; **TRUNCATE withheld separately from DELETE**; `REVOKE ALL FROM PUBLIC` first
- [X] T049 [P] Integration test asserting the application role cannot UPDATE or DELETE an audit row in `tests/Integration/Audit/AuditImmutabilityTests.cs` — 7 tests **connecting as the restricted role, not the superuser**, asserting SQL state 42501 on UPDATE/DELETE/TRUNCATE while INSERT and SELECT succeed

### Seed and reverse proxy

- [X] T050 [P] Create the seeder tool for development data and load-test volume in `tools/Seeder/Program.cs` — `--dev` seeds 20 employees (the roster T061's Keycloak realm must mirror, joined on `external_subject`), 10 direct conversations, 2 groups, and 400 messages, one group deliberately over the 50-row history page so keyset pagination is exercised by just opening it. All ids are derived rather than random, so re-running appends nothing — **verified: a second run left 20/12/37/400 unchanged**. `--load N` bulk-loads via binary `COPY` at **~80,000 rows/s measured** (125 M ≈ 26 min), spread over the 12-month retention window and calling T026's `internalchat_ensure_month_partitions` first. Bodies come from a skewed bilingual vocabulary because a corpus of identical strings would make the D9 search budget pass while measuring nothing — **verified: 138 k distinct bodies in 200 k rows across 13 partitions, a rare term matching 7,409**. ⚠️ The tables it writes belong to T060/T088, so a schema preflight names the missing table and its owning task instead of failing on raw SQL
- [X] T051 Configure TLS termination and the internal-only attachment location in `deploy/nginx/nginx.conf` — TLS 1.2/1.3, `location /internal-attachments/ { internal; }` reachable only via `X-Accel-Redirect` after the API's membership check (FR-025), range requests passed through for FR-022, `Content-Disposition: attachment` always. Dev certs via `deploy/scripts/generate-dev-certs.sh` (git-ignored)

**Checkpoint**: Foundation ready (T016–T051) — user story implementation can now begin. Solution
builds with zero warnings under `TreatWarningsAsErrors`; 20 architecture, 4 unit, and 42 integration
tests pass, the latter against real PostgreSQL, Redis, RabbitMQ, MinIO, and Keycloak containers.

Two things carried into Phase 3 rather than fixed here:

- The `message` table does not exist yet, so the seeder's insert paths were verified against a
  hand-built stand-in schema, not the real migration. T088 must re-run `--dev` and `--load` once
  the real table lands.
- `body_tsv` as `GENERATED ... to_tsvector('simple', unaccent(body))` **will be rejected**:
  `unaccent` is STABLE, not IMMUTABLE, and a generated column requires IMMUTABLE. T163 needs an
  IMMUTABLE wrapper function. Found while building the stand-in schema.

---

## Phase 3: User Story 1 - Secure Sign-In and Access Control (Priority: P1) 🎯 MVP

**Goal**: Only active employees reach the platform, they see only their own conversations, and
deactivation revokes access within 5 minutes including for an already-open connection.

**Independent Test**: Sign in with corporate credentials and reach a seeded conversation; request a
conversation you do not belong to and be refused; deactivate the employee in Keycloak and confirm
an already-open session stops working within 5 minutes.

### Tests for User Story 1 (REQUIRED) ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [X] T052 [P] [US1] Unit tests for Employee invariants — a deactivated employee cannot be added to a conversation — in `tests/Unit/Domain/EmployeeTests.cs` — 13 tests. Beyond the stated invariant they pin the idempotency directory sync depends on: a redelivered deactivation must not move `deactivated_at`, because that timestamp is what an auditor reads to establish when access ended (SC-021). Also asserts attribute sync can never change `Status` — a rename and a revocation must not be the same operation when only one has a 5-minute budget
- [X] T053 [P] [US1] Unit tests for the authorization evaluator — deny by default, active membership required — in `tests/Unit/Application/ConversationMembershipEvaluatorTests.cs` — 10 tests written around the failure modes rather than the happy path. Includes the two that matter most: a reader failure **refuses** rather than allowing (SC-024 — an outage may cost latency, never a wrong access decision), and `OperationCanceledException` is not swallowed into a denial, which would fill the audit trail with intrusion-shaped noise every time a browser tab closed. A separate test pins `Member < Admin` because the evaluator compares roles with `<`
- [X] T054 [P] [US1] Contract tests for `/me`, `/me/sessions`, `/directory/employees` against `contracts/openapi.yaml` in `tests/Contract/DirectoryContractTests.cs` — 9 tests. The document is **parsed** (YamlDotNet, MIT, added to the licence register) rather than restated in C#, because a test comparing code to a copy of itself passes forever. Asserts the routing table offers every documented path and method, and that response DTOs serialize every required property with the contract's spelling — obtained by serializing an instance, so the camel-case policy is part of what is asserted. **Found a real defect**: `MapGroup("/me")` + `MapGet("/")` produced `/api/v1/me/`, so a generated client calling the documented `/api/v1/me` would have got a 404
- [X] T055 [P] [US1] Integration test asserting a refusal is indistinguishable from a not-found in both body and timing in `tests/Integration/Authorization/RefusalOpacityTests.cs` — 5 tests. Bodies compared field by field with the trace id removed, not merely by status code; timing compared on **medians over 30 samples** with a 50 ms tolerance, deliberately loose because the target is a structural difference (an extra round trip) rather than a few milliseconds of scheduler noise. Also pins the case where a live membership grants nothing because the employee was deactivated — the join an authorization read is most likely to omit. **Mutation-verified**: making `UnauthorizedAccessException` return a 403 with its own title failed the suite. ⚠️ Runs against `ProtectedProbeFactory`, which hosts a conversation-scoped endpoint using the real filter, evaluator, and exception handler in `Program.cs`'s middleware order — the real `GET /conversations/{id}` is T095, and **that task should re-point these tests at it and delete the probe**
- [X] T056 [P] [US1] Integration test asserting deactivation closes an already-open SignalR connection within 5 minutes in `tests/Integration/Authorization/RevocationTests.cs` — 4 tests over a genuine WebSocket handshake through the real pipeline. The client sends nothing between revocation and closure, which is the case a hub filter cannot cover and SC-018 actually tests. Includes two controls that stop it passing for the wrong reason: an unrevoked connection survives several sweeps, and revoking one employee leaves another connected
- [X] T057 [P] [US1] Integration test asserting a reconnect with an expired token is refused in `tests/Integration/Authorization/ReconnectValidationTests.cs` — 4 tests. Tokens are **really expired**: the realm's lifespan is narrowed through the Keycloak admin API, a real token is issued, and the setting restored, so Keycloak's own signature and `exp` are what get validated. Two traps recorded: the first version asserted "any exception" from `StartAsync` and passed against an API with **no hub mapped at all** (now asserts a 401 specifically), and the realm's lifespan could not be narrowed while the test client pinned `access.token.lifespan`, which is why neither client in the realm export carries that attribute

### Implementation for User Story 1

- [X] T058 [P] [US1] Create `Employee`, `EmployeeStatus`, and `Presence` in `src/InternalChat.Domain/Employees/` — the deactivated-employee check lives on the entity (`EnsureCanJoinConversation`) rather than in a use case, so every path that adds a member passes the same gate instead of each new one having to remember. Deactivation and reactivation raise events carrying `external_subject` as well as the internal id, because the revocation set is keyed by the token's `sub` claim and that path has the tightest deadline in the system
- [X] T059 [P] [US1] Create `Membership` with role, `visible_from_seq`, and `removed_at` in `src/InternalChat.Domain/Conversations/Membership.cs` — deliberately not an `Entity<TId>`: the key is the composite `(conversation_id, employee_id)`, and a surrogate id added only to satisfy a base class invites code to reference a membership by something other than the pair that defines it. `Rejoin` takes `Math.Max` of the old and new floor so a re-added member never regains history they were removed from
- [X] T060 [US1] Add EF configurations and a migration for `employee` and `membership` in `src/InternalChat.Infrastructure/Persistence/` — **verified against real PostgreSQL**: `citext` email, GIN trigram on `display_name` for FR-007, and the authorization lookup index made partial (`WHERE removed_at IS NULL`) so a year of membership churn does not come to dominate an index read that happens on every request. No FK to `conversation` yet — that table is T088; the constraint lands there. Two traps found and recorded: declaring the PG enums in *both* `HasPostgresEnum` and `MapEnum` emits duplicate annotations with **conflicting label order** (`membership_role` came out as both `member,admin` and `admin,member` in one migration), and `dotnet ef` with `--no-build` silently uses a stale assembly, writing a migration that does not match the snapshot. ⚠️ PostgreSQL ordered the enum labels alphabetically, so `membership_role` is `admin(1), member(2)` — the reverse of the C# ordering. Harmless today since values store by label, but `ORDER BY role` in SQL would mean the opposite of what it reads like; documented in the migration
- [X] T061 [US1] Create the Keycloak realm export with 20 development employees, roles, and the SPA client in `deploy/keycloak/realm-export.json` — generated from `DevelopmentSeeder.Roster` so the two cannot drift, plus `deploy/keycloak/README.md`. **User ids are `dev-{username}`, not UUIDs**: `sub` becomes `employee.external_subject`, and a random id would produce a realm whose users sign in perfectly and match no employee row. **Verified against a real Keycloak 26.7.1 container**: realm imports, `sub` is `dev-an.nguyen`, `aud` is `internalchat-web` via the audience mapper (Keycloak emits only `azp` otherwise), `sid` present, `exp - iat` = 300 s, and the Vietnamese display name survives as a `display_name` claim. Neither client sets `access.token.lifespan` — a client-level value silently overrides the realm's, which made the realm setting both decoration and untestable. `internalchat-test` (direct access grants) is development-only and the README says so
- [X] T062 [US1] Configure JWT bearer validation of signature, issuer, audience, and expiry in `src/InternalChat.Api/Program.cs` — via `Api/Authorization/AuthenticationExtensions.cs` and `ChatAuthenticationOptions`. **`ClockSkew = TimeSpan.Zero`**: the 5-minute default would let a 5-minute token be accepted for ten, silently doubling the window FR-003 gives to end access. Inbound claim mapping is switched off so `sub` stays `sub` rather than becoming a WS-Federation URI that no code looks for; `realm_access.roles` is flattened into role claims, because Keycloak emits them as a JSON blob that `RequireRole` compares as a string and never matches. Query-string tokens are accepted on the hub path **only** — the browser WebSocket API cannot set headers, and allowing it everywhere would put access tokens in every proxy log
- [X] T063 [US1] Implement the SignalR hub filter validating the token on connect **and** reconnect in `src/InternalChat.Api/Authorization/HubAuthorizationFilter.cs` — plus `HubConnectionRegistry` and `RevocationSweepService`, and a minimal `Hubs/ChatHub.cs` (US1 needs a connection to close; T097 adds the methods). The filter and the sweep are **not redundant**: the filter is immediate but only fires when the client invokes something, and an idle client invokes nothing — which is exactly the case SC-018 tests. A connection whose principal carries no subject is refused rather than registered, since it could never be swept
- [X] T064 [US1] Implement the Redis revocation set and the Keycloak back-channel logout endpoint in `src/InternalChat.Infrastructure/Identity/RevocationStore.cs` — plus `Endpoints/AuthEndpoints.cs`, `Authorization/LogoutTokenValidator.cs`, and `Identity/SessionRegistry.cs` for FR-005. Revocation is keyed **both** by session (sign-out) and by subject (deactivation cannot enumerate sessions it has never seen). Entries deliberately outlive the tokens they revoke; a shorter TTL would let a revoked token start working again, silently. `IsRevokedAsync` is the **one place in this codebase that fails open**, with the reasoning written down: the token is already cryptographically validated and has ≤5 minutes left, and failing closed would sign 10,000 employees out the moment Redis restarted (SC-024). The logout endpoint is anonymous by necessity — added to `JustifiedAnonymousRoutes` — so the logout token is validated in full, including the `events` claim and the absence of `nonce`, without which any access token could be replayed to sign its bearer out
- [X] T065 [US1] Implement the directory sync consumer performing employee upsert and deactivation in `src/InternalChat.Worker/Consumers/DirectorySyncConsumer.cs` — plus the `SyncEmployee` use case, `IEmployeeStore`, and `chat.directory.employee.changed.v1` added to `contracts/messaging.md` with queue `directory.sync` at **prefetch 1**, because a deactivation overtaken by a stale upsert would silently restore access. 8 integration tests against real PostgreSQL and Redis. Two structural fixes were needed to get here: `IMessageConsumer`/`MessageEnvelope` **moved from Infrastructure to Application** (a Worker consumer implementing an Infrastructure interface fails `CompositionRootTests`), and `ConsumerHost` now resolves the handler from the **per-message scope** — it previously reused the instance passed to `StartAsync`, so a consumer with scoped dependencies would have written through a different `DbContext` than the transaction holding its dedup record, and the split would only have shown up when a handler threw
- [X] T066 [US1] Implement `ConversationMembershipRequirement` and its evaluator in `src/InternalChat.Application/Authorization/` — single `Allow` return at the end of the method so a branch added later falls through to the refusal rather than past it, which is the classic authorization bug. An unavailable dependency is a denial (`Undetermined`), never a pass-through. Caching is deliberately not here: `IMembershipReader`'s implementation owns the 30 s cache (T068), leaving the decision a pure function of what the reader returns
- [X] T067 [US1] Implement the endpoint filter applying the membership policy in `src/InternalChat.Api/Authorization/MembershipEndpointFilter.cs` — a refusal is **thrown**, not returned, so `ProblemDetailsHandler` shapes every one identically to a not-found in one place and no endpoint can get it subtly wrong (SC-017). The conversation id is read from route values only: accepting it from a query string or header would let a caller pass the check for one conversation and be served another, which is the exact failure resource-scoping exists to prevent
- [X] T068 [US1] Implement the 30-second membership cache with an explicit invalidation contract in `src/InternalChat.Infrastructure/Caching/MembershipCache.cs` — TTL bound to `Authorization:MembershipCacheTtlSeconds` and **refused at startup** above the 60 s ceiling, because a cache TTL is exactly the number that gets raised during an incident and never lowered. Both layers apply the same filter, including the join to `employee`, so deactivation takes effect on the authorization path without any caller remembering to check it. **Misses are cached too** — otherwise probing conversations you do not belong to reaches PostgreSQL every time, which is both the cheapest denial of service available and a timing signal about which conversations exist
- [X] T069 [US1] Implement `GET /me` including `canReceiveNotifications` in `src/InternalChat.Api/Endpoints/MeEndpoints.cs` — returns `false` with reason `no_subscription` until push lands in US4 (T130, T136). That is the honest answer today, and FR-040 requires saying so rather than letting an employee assume they are reachable
- [X] T070 [US1] Implement `GET` and `DELETE /me/sessions` in `src/InternalChat.Api/Endpoints/MeEndpoints.cs` — backed by `ISessionRegistry` over a Redis hash rather than the Keycloak admin API, which would put an admin round trip on a user-facing path and require the API to hold admin credentials to serve a read-only list. Expiry is logical as well as physical: one live session refreshes the hash, so reads also drop entries older than the TTL — otherwise the list would show an abandoned device to the employee checking for exactly that. Delete revokes as well as forgets; forgetting alone would drop a session from the list while its token kept working
- [X] T071 [US1] Implement `GET /directory/employees` with a trigram index in `src/InternalChat.Api/Endpoints/DirectoryEndpoints.cs` — `ILike` served by the GIN trigram index from T060, LIKE metacharacters escaped so a search for `100%` does not match everyone, limit clamped in the repository as well as the endpoint so a future caller cannot ask for ten thousand rows by not knowing the contract
- [X] T072 [US1] Emit audit events for sign-in, sign-out, and access denial in `src/InternalChat.Application/Behaviors/AuditBehavior.cs` — added `ISecurityAuditor`/`SecurityAuditor` alongside the pipeline behavior. The two are deliberate mirrors: pipeline audits commit **inside** the use case's transaction so an action and its record are atomic, while these commit **outside** any transaction so a rollback cannot take the evidence of a refusal with it. Sign-in fires once per session (first sight in the registry), not once per request — SC-021 needs a log a person can still read a year later
- [X] T073 [P] [US1] Implement the OIDC authorization-code-with-PKCE flow in `src/internalchat-web/src/lib/auth/` — `pkce.ts`, `oidcClient.ts`, `config.ts`. Hand-rolled rather than `oidc-client-ts`: PKCE is a random string, a SHA-256, and base64url, and Principle VIII prefers one fewer dependency to re-verify. Verifier from `crypto.getRandomValues`, S256 only, state checked on return, verifier and state in `sessionStorage` and cleared on failure. Sign-out goes through the end-session endpoint — dropping local tokens leaves the Keycloak session open, so an employee who "signed out" on a shared machine did not, and it is also what triggers the back-channel logout that writes the revocation set
- [X] T074 [P] [US1] Implement protected routes and silent token refresh in `src/internalchat-web/src/lib/auth/TokenRefresh.ts` — plus `AuthProvider.tsx` and `authContext.ts`. Refreshes 60 s ahead of expiry with a 5 s floor (without it, a token already inside the margin schedules at zero and spins), single-flight so a burst of parallel requests cannot redeem a rotating refresh token twice and invalidate the chain, and `exp` from the token preferred over `expires_in` because the API validates `exp` with zero skew. `RequireAuth` renders nothing until signed in, so "protected" is a property of the tree
- [X] T075 [P] [US1] Vitest tests for the auth library in `src/internalchat-web/tests/auth.test.ts` — 20 tests covering the silent failures: a non-random verifier, the missing state check (a link carrying an attacker's code), a verifier surviving a failed exchange, the refresh loop, the double redemption, and `expires_in` overriding `exp`. Frontend gates all green: strict typecheck, ESLint 0 errors 0 warnings, Prettier, and a 63 kB gzipped bundle against the 300 kB budget
- [X] T076 [US1] Playwright E2E covering quickstart V1 in `tests/e2e/v1-signin-revocation.spec.ts` — 3 tests, one per quickstart step, plus `playwright.config.ts`, `keycloakAdmin.ts`, and a README. Deactivation is performed by **disabling the realm user**, which makes Keycloak end the sessions and call the back-channel logout endpoint — the path a real departure takes, not a test-only shortcut. `workers: 1` and `retries: 0`, because two workers would revoke each other's sessions and a retried end-to-end test is how a real race gets filed as "just flaky". ⚠️ **Written and type-checked; the spec parses and enumerates, but it has not been executed against a running stack** — that needs `docker compose up` with images built (T013 and T027 both record images as not yet built), and it is what T219 exists to do

**Checkpoint**: Authentication and authorization work end to end (T052–T076). The refusal-opacity
and revocation tests pass, so US2 may begin. Backend suite: 27 unit, 20 architecture, 9 contract,
and 63 integration tests green, the last against real PostgreSQL, Redis, RabbitMQ, MinIO, and
Keycloak — with the development realm imported, so the authentication path is exercised against the
configuration the deployment actually runs. Frontend: 20 Vitest tests, strict typecheck, ESLint
clean, 63 kB gzipped.

Three things carried into Phase 4 rather than fixed here:

- **T055 runs against a probe host.** `ProtectedProbeFactory` supplies the conversation-scoped
  endpoint the opacity test needs, using the real filter and exception handler in `Program.cs`'s
  order. T095 must re-point those tests at the real `GET /conversations/{id}` and delete the probe,
  or it becomes a second composition root that quietly diverges.
- **T076 has not been run.** The spec parses and enumerates; executing it needs the Compose images,
  which T013 and T027 both record as unbuilt. T219 is where it runs.
- **`GET /me` reports `canReceiveNotifications: false` for everyone**, because there is nowhere to
  store a push subscription until T130. Honest today, and T136 must revisit it.

---

## Phase 4: User Story 2 - One-to-One Direct Messaging (Priority: P1) 🎯 MVP

**Goal**: Two employees exchange messages in real time, exactly once, in a stable order, with
history that loads progressively and survives reconnects.

**Independent Test**: Two browsers, one direct conversation; every message appears on the other
side without a refresh; a retried send produces exactly one message; three messages sent across a
network drop all arrive once, in order.

### Tests for User Story 2 (REQUIRED) ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

**Phase 4 progress (paused mid-T089/T098 wiring).**

🔴 **THE TREE DOES NOT RUN RIGHT NOW — FIX THIS FIRST.** `Messaging.ConsumerHost` is registered
in no DI container. It never was; the Worker's `QueueConsumerService` would have failed to start
too, and nothing caught it because no test has ever started the Worker host. It surfaced when
`QueueConsumerService` was added to the API host for T098. The build is clean but the API host
cannot be constructed, so **4 architecture and 13 contract tests fail on host startup**. The fix
is one `TryAddScoped<Messaging.ConsumerHost>()` in `AddMessaging`, after checking its
constructor — but verify the Worker actually starts as well, because that host has no test
covering startup at all.

Written and green: T077–T079, T086–T088.

Written and **deliberately red**, awaiting the implementation they specify: T080, T081 (8
failures — unmapped routes and undeclared hub methods), T082–T085 (22 failures — 404s and
"hub method does not exist"). All verified to fail for the right reason, not on infrastructure.

Implementation written but **not yet verified against the failing tests** (blocked by the startup
defect above): T089 (`PartitionMaintenanceJob`, `IPartitionMaintenance`), T090 (conversation,
message, membership repositories), T091–T094 (`CreateConversation`, `SendMessage`, `GetHistory`,
`EditMessage`/`DeleteMessage`, plus `ListConversations` and `ResyncConversations`), T095–T099
(conversation and message endpoints, `ChatHub` methods, `RealtimeFanoutConsumer`,
`PresenceStore`). Not started: T100–T108 (frontend, E2E, load).

**Three defects found in already-completed work, all of the same kind — wiring that no test
exercised:**

1. **RabbitMQ bindings matched almost nothing (T036).** AMQP `*` matches exactly one word, and
   routing keys are event types carrying a `.v1` suffix — so `chat.directory.*` cannot match
   `chat.directory.employee.changed.v1`. Four of seven queues were affected. `directory.sync`
   received **nothing**, which means deactivation would never have reached a consumer and
   FR-003's five-minute revocation deadline would have been missed silently on every departure.
   Nothing failed visibly: an exchange discards an unroutable message, the publisher confirm
   still succeeds, and the outbox row is still marked dispatched. Every binding is now `#`, and
   `tests/Integration/Messaging/TopologyRoutingTests.cs` (8 tests, green) publishes every known
   event type through a real broker and asserts which queues receive it. `contracts/messaging.md`
   updated, including a note that a redeploy leaves the old bindings behind for the runbook to
   clean up.
2. **The outbox was never drained (T037).** `OutboxDispatcher` was built and tested but never
   hosted — `Program.cs` carried a `// T037 wiring` comment. Every real-time delivery,
   notification, and search update in the platform is downstream of that loop, so US2 could not
   have worked end to end. `OutboxDispatcherService` now hosts it with adaptive polling.
3. **`ConsumerHost` unregistered** — the item at the top of this note.

Also landed: the **`membership` → `conversation` foreign key** T060 deferred to T088. It made six
test call sites unrepresentable — they seeded memberships against conversations that were never
created — so `TestData.SeedMembershipAsync` now creates the conversation first. Not a workaround:
a membership is the authorization record *for a conversation*, and one naming a conversation that
does not exist grants access to nothing.

Two contract changes, both additive and both forced by the partition key: `MessageSent`,
`MessageEdited`, and `MessageDeleted` now carry `SentAt`, without which a consumer holding only a
message id must scan all sixteen monthly partitions to read one row. `Conversation.muteduntil`
was corrected to `mutedUntil` while the field is still unimplemented.

- [X] T077 [P] [US2] Unit tests for message body length, required ULID key, and the 24-hour edit/delete window in `tests/Unit/Domain/MessageTests.cs` — 15 tests. Beyond the stated limits they pin the boundaries that get rounded the wrong way: the length limit is measured **after** trimming, the 24-hour window boundary is inclusive, and a second delete does not move `deleted_at` — the same idempotency reasoning as T052, since that timestamp is what an auditor reads to establish when the content stopped being available. Also asserts the sent event carries **no body**, which is FR-056 enforced at the point the payload is built rather than in the dispatcher
- [X] T078 [P] [US2] Unit tests asserting ordering is derived from the server sequence and never from a client clock in `tests/Unit/Domain/MessageOrderingTests.cs` — 6 tests. The load-bearing one runs the clock **backwards** between two sends and asserts the sequence still ascends: a test that only checks ordering under a forward-running clock passes just as happily against an implementation that sorts by timestamp, which is exactly what FR-012 forbids. Also pins that an edit does not change the ordering key
- [X] T079 [P] [US2] Unit tests for direct-conversation invariants — exactly two members, neither removable — in `tests/Unit/Domain/ConversationTests.cs` — 13 tests. Includes the self-conversation case (one employee passed twice satisfies a naive count of two), that the direct key is identical whichever participant asks first — the property the whole dedup index rests on — and that it matches the spelling `tools/Seeder` builds, so the two cannot drift into seeded conversations that deduplication cannot see
- [X] T080 [P] [US2] Contract tests for the conversation and message endpoints against `contracts/openapi.yaml` in `tests/Contract/MessagingContractTests.cs` — ⚠️ **written and observed failing; completes with T095/T096.** 13 tests, 7 of them the routing assertions that fail today because no endpoint is mapped yet. The DTO half already passes, which is what makes the remaining failures meaningful rather than noise. Beyond required-property checks it pins `seq` as `int64` and `body` as nullable — both mistakes that produce a working system for months and then a broken one, since an `int32` sequence is fine until a conversation overflows it and a non-nullable body makes the *first* delete in production fail inside serialization. Also asserts the `from_join` spelling, because a client comparing against the C# member name `FromJoin` falls through to its "show everything" branch and tells a new member they can read history US3 scenario 4 says they cannot. **Found and fixed a typo in the contract**: `Conversation.muteduntil` should be `mutedUntil` — corrected now, while the field is still unimplemented and the rename costs nothing, rather than after US4 ships a client reading it — 15 tests. Route comparison normalises ASP.NET Core's inline constraints (`{conversationId:guid}`) against the contract's `{conversationId}`: the first version compared raw strings and reported six correctly-mapped operations as missing. Stripping the constraints from the endpoints to satisfy the test would have traded a real input check for a green comparison.
- [X] T081 [P] [US2] Contract tests asserting SignalR events match `contracts/signalr-hub.md` in `tests/Contract/HubContractTests.cs` — ⚠️ **written and observed failing; completes with T097/T098.** 12 tests. SignalR has no schema language, so the Markdown contract's two tables are **parsed** rather than restated in C# — the same "comparing code to a copy of itself" trap `OpenApiContract` exists to avoid. This matters more here than for HTTP: SignalR binds **by name at runtime**, so a renamed hub method produces no compiler error, no 404, and no log entry — the client's call simply never completes, and a renamed event is delivered to nobody. Both directions are asserted, including that the hub declares no method the contract does not document, since every hub method is a remotely invocable entry point. `ChatHubEvents` constants added so the fan-out consumer and this test cannot disagree. One test pins that the hub offers **no** `SendMessage`: the contract puts sends on HTTP deliberately, and a hub-side send would duplicate retry semantics onto a transport with no status codes to express "already accepted, here is the original" — 19 tests over the parsed hub contract. Caught that `DeclaredOnly` reflection is not enough to find client-callable methods: an **override** is declared on the derived type, so `OnConnectedAsync` was reported as an undocumented entry point. Comparing against `GetBaseDefinition()` separates a new hub method from an override of a `Hub` member.
- [X] T082 [P] [US2] Integration test asserting a duplicate `clientMessageKey` returns 200 and leaves exactly one row in `tests/Integration/Messages/IdempotentSendTests.cs` — 5 tests. **Found a real defect**: `sent_at` returned by a send did not equal the one returned by its replay, because the first came from memory and the second from PostgreSQL, whose `timestamptz` holds microseconds against `DateTimeOffset`'s 100-ns ticks. Not cosmetic — every event carries `SentAt` so a consumer can find the row by `(id, sent_at)`, and an equality filter one tick off matches nothing, so real-time fan-out would have delivered silently nothing. Fixed at the source in `ClockResolution`.
- [X] T083 [P] [US2] Integration test asserting keyset pagination is correct across a monthly partition boundary in `tests/Integration/Messages/HistoryPaginationTests.cs` — 6 tests across a real monthly partition boundary, including the caller's `visible_from_seq` floor.
- [X] T084 [P] [US2] Integration test asserting `Resync` returns everything above `lastSeenSeq` exactly once in `tests/Integration/Messages/ResyncTests.cs` — 7 tests. Covers the bounded page for a very stale floor, so a client returning after a month cannot ask for an unbounded replay.
- [X] T085 [P] [US2] Integration test asserting two concurrent direct-conversation creations produce one conversation in `tests/Integration/Conversations/DirectDedupeTests.cs` — 5 tests, up to eight simultaneous attempts. **Two genuine concurrency bugs found and fixed here**, both invisible to a single-threaded test: the handler did check-then-insert, so all eight callers missed the check, all eight inserted, and seven died on the unique index as HTTP 500s; and after moving to `INSERT ... ON CONFLICT`, merging the fallback read into the same statement failed roughly one attempt in eight, because `ON CONFLICT` waits for the conflicting transaction but a READ COMMITTED statement's snapshot is taken *before* that wait — so the loser looked for the winner's row and could not see it. The read has to be a separate statement.

### Implementation for User Story 2

- [X] T086 [P] [US2] Create `Message`, `MessageBody`, `ClientMessageKey`, and `EditWindow` in `src/InternalChat.Domain/Messages/` — `Send` takes no timestamp parameter at all, so FR-012's "server clock, never the client's" is enforced by the shape of the API rather than by callers remembering. Deletion is soft and terminal: the row survives with its body cleared, because a gap in the sequence would make "everything after seq N" ambiguous on every `Resync`. Mentions are resolved once at send and stored, not re-derived on read — re-resolving later would change who a past message notified as people join and leave
- [X] T087 [P] [US2] Create `Conversation` with `direct_key`, `last_seq`, and `HistoryVisibility` in `src/InternalChat.Domain/Conversations/Conversation.cs` — `LastSeq` starts at **zero, not one**, so a member joining an empty conversation gets a floor of 0 and the first message (seq 1) passes the `seq > visible_from_seq` filter; starting at one would hide the first message from everyone. A direct conversation is created `Full` rather than `FromJoin` — both participants were present from the first message by construction, so there is no join point worth hiding anything behind. `EnsureMembersMayChange` is one gate for both add and remove, since "exactly two members, neither removable" is a single invariant and splitting it invites one half to be forgotten
- [X] T088 [US2] Add EF configurations and a migration for `conversation` and partitioned `message`, including the unique indexes on `(conversation_id, client_message_key)` and `(conversation_id, seq)` in `src/InternalChat.Infrastructure/Persistence/` — **the two unique indexes data-model.md specifies cannot both exist as written, and finding out why changed the design.** PostgreSQL refuses a unique constraint on a partitioned table that omits the partition key, and widening `(conversation_id, client_message_key)` to include `sent_at` would be worse than useless: a retried send gets a *different* server timestamp, so the composite differs and the duplicate is admitted — an index that looks like the exactly-once guarantee and enforces nothing. FR-011 therefore lives on its own un-partitioned `message_dedup` table whose PK is exactly `(conversation_id, client_message_key)`, carrying `message_id` so a collided retry returns the winning message rather than failing, and `sent_at` so the retention sweep can delete by the same month range it drops a partition for. `(conversation_id, seq)` is a plain index for the same partition-key reason; uniqueness there is already guaranteed upstream by the row-locked allocator. The table is created by hand-written SQL because EF Core cannot express `PARTITION BY RANGE`. **Verified against real PostgreSQL**: `relkind=p` with 16 partitions spanning the 12-month retention window plus three ahead, two messages in different months routed to different partitions, a duplicate dedup key refused, the body/tombstone check firing on the partition, and an insert into an unpartitioned month failing outright — which is what makes T089 load-bearing rather than housekeeping
- [X] T089 [US2] Implement the monthly partition creation job in `src/InternalChat.Worker/Jobs/PartitionMaintenanceJob.cs` — monthly partition creation, running ahead of the boundary. An `INSERT` into a month with no partition fails outright rather than falling back to the parent, so a boundary crossing with none ready would reject every send on the platform.
- [X] T090 [US2] Implement `IMessageRepository` and `IConversationRepository` in `src/InternalChat.Infrastructure/Persistence/Repositories/` — plus `IConversationReader`, split from the write side for the same reason `IEmployeeDirectory` is split from `IEmployeeStore`. Sequence allocation is `UPDATE ... RETURNING` under a row lock (D1); read-then-write would leave a window in which two senders take the same number.
- [X] T091 [US2] Implement the `CreateConversation` use case with direct deduplication via `direct_key` in `src/InternalChat.Application/Conversations/CreateConversation.cs` — deduplicates on `direct_key` through a single `INSERT ... ON CONFLICT DO NOTHING`, then reads the winner in a separate statement. The `SELECT` first is kept as a cheap fast path for the common case, but it is explicitly not the mechanism — see T085 for what happens when it is.
- [X] T092 [US2] Implement the `SendMessage` use case — sequence allocation, message insert, and outbox write in one transaction, returning the existing message on key collision — in `src/InternalChat.Application/Messages/SendMessage.cs` — sequence allocation, message insert, dedup reservation, and outbox write in one transaction. The key is reserved **before** the message is built, so a retry is answered without allocating a sequence that a rollback would then have to give back.
- [X] T093 [US2] Implement the `GetHistory` use case with keyset pagination and the `visible_from_seq` floor in `src/InternalChat.Application/Messages/GetHistory.cs` — keyset pagination on `(conversation_id, seq)`, floored by the caller's `visible_from_seq`. The floor is applied in the query rather than after it: a UI-level filter is a display preference, and this is an access rule.
- [X] T094 [US2] Implement the `EditMessage` and `DeleteMessage` use cases enforcing the 24-hour window in `src/InternalChat.Application/Messages/` — both enforce the window on the entity, so the endpoint, the hub, and anything added later pass the same gate. A deleted message is terminal and cannot be edited back into existence.
- [X] T095 [US2] Implement the conversation endpoints in `src/InternalChat.Api/Endpoints/ConversationEndpoints.cs` — list, create, and detail. `POST` answers 201 for a new conversation and 200 for an existing direct one, as the contract documents.
- [X] T096 [US2] Implement the message send, history, edit, and delete endpoints in `src/InternalChat.Api/Endpoints/MessageEndpoints.cs` — send, history, edit, delete. Send answers 201 then 200 on replay, which is how a client tells a new message from its own retry.
- [X] T097 [US2] Implement `ChatHub` with `Resync`, `StartTyping`, `StopTyping`, and `SetPresence`, delegating every method to a use case, in `src/InternalChat.Api/Hubs/ChatHub.cs` — `Resync`, `StartTyping`, `StopTyping`, `SetPresence`, each delegating to a use case. `OnConnectedAsync` joins the caller's conversation groups and asserts presence, because a client that connected and then crashed would otherwise never appear online at all.
- [X] T098 [US2] Implement the real-time fan-out consumer emitting `MessageReceived`, `MessageEdited`, and `MessageDeleted` in `src/InternalChat.Api/Consumers/RealtimeFanoutConsumer.cs` — consumes `chat.message.sent.*` in the API and emits the SignalR events. Looks the message up by `(id, sent_at)`, which is why the events carry `SentAt` and why T082's tick mismatch mattered.
- [X] T099 [US2] Implement presence and typing Redis keys with TTL expiry rather than explicit cleanup in `src/InternalChat.Infrastructure/Caching/PresenceStore.cs` — presence and typing expire by TTL, never by explicit cleanup: a crashed client must not leave a permanent "typing" state.
- [X] T100 [P] [US2] Build the conversation list with TanStack Query in `src/internalchat-web/src/features/conversations/` — TanStack Query owns fetching and caching, with `refetchOnWindowFocus` off: a list refetched on every tab switch is a request per switch all day, and real-time events invalidate the key when it actually changes. Cache keys live in their own module so the component file exports components only — Fast Refresh silently stops working for a module that mixes the two.
- [X] T101 [P] [US2] Build the virtualized message list with `react-virtuoso` in `src/internalchat-web/src/features/messages/MessageList.tsx` — `react-virtuoso`, sorted by `seq` and never by `sentAt` (FR-012). Pending sends are keyed by `clientMessageKey`, which is the key the confirmed message will also carry, so the optimistic bubble is replaced rather than duplicated. A deleted message renders as a tombstone, because an empty bubble reads as a rendering bug.
- [X] T102 [P] [US2] Build the composer with optimistic send and an offline queue keyed by ULID in `src/internalchat-web/src/features/messages/Composer.tsx` — the component holds no send logic; `OfflineQueue` owns the key, the ordering, the single-flight flush, and the persistence. The composer stays usable while offline, deliberately: the queue is what delivers the message, so disabling the input would remove the one thing that still works — and quickstart V2 step 3 requires typing three messages with the network down.
- [X] T103 [P] [US2] Build the SignalR client with automatic reconnect and `Resync` in `src/internalchat-web/src/lib/realtime/` — reconnect and resync cannot be separated. `Resync` runs after `start()` as well as after every reconnect, because the first connection of a session has a gap too. Built inside an effect rather than a `useMemo`: a memo is a hint React may discard, and discarding one that owns a socket opens a second connection and leaks the first.
- [X] T104 [P] [US2] Build typing indicators and presence display in `src/internalchat-web/src/features/messages/TypingIndicator.tsx` — typing state is held per conversation so switching conversation does not rebuild the connection. Presence puts its state in the accessible name rather than in colour alone (WCAG 1.4.1); typing is `aria-live="polite"` so a screen reader mentions it between utterances instead of interrupting the message being read.
- [X] T105 [P] [US2] Vitest tests asserting the offline queue never sends a duplicate after reconnect in `src/internalchat-web/tests/offline-queue.test.ts` — 10 tests. Asserts the key is identical across every retry, order survives a reconnect through storage, the flush stops at the first failure so a later message cannot take a lower sequence, flushes are single-flight, a permanently rejected message does not block the queue, and keys survive a reload.
- [X] T106 [US2] Playwright two-client delivery timing harness measuring SC-006 in `tests/e2e/v2-delivery-timing.spec.ts` — 5 tests across two specs. Measures delivery p95/p99 over 20 samples between two independent browser contexts, and covers quickstart V2 step 3 end to end with a real `setOffline`. ⚠️ **Written and enumerated, not executed** — needs the Compose images (T013, T027).
- [X] T107 [US2] k6 script for the send and history budgets in `tests/Load/messaging.js` — arrival-rate scenarios holding 100 sends/s then bursting to 1,000/s, with history read concurrently because in production they compete for the same connection pool. Thresholds are k6 `thresholds`, so a breach exits non-zero and the script is usable as CI gate 6. 429 is recorded as a correct answer under burst rather than a failure — the rate limiter is doing its job.
- [X] T108 [US2] Verify budgets against a database seeded to full retention volume, not an empty one, in `tests/Load/README.md` — documents the rule, the method, and how to keep the 26-minute seed as a volume snapshot so nobody runs against a partially seeded database. Also records *why* an empty database gives a false pass: the planner picks a sequential scan when it is genuinely cheaper, so the test validates the opposite of production's plan. ⚠️ **No number in plan.md's budget table has been verified yet** — T218 runs it.

**Checkpoint**: **MVP feature-complete** (T077–T108). Employees can sign in and exchange direct
messages. Backend: 68 unit, 20 architecture, 34 contract, and 93 integration tests green across
three consecutive full runs, the integration suite against real PostgreSQL, Redis, RabbitMQ, MinIO,
and Keycloak. Frontend: 30 Vitest tests, strict typecheck, ESLint clean, 109 kB gzipped against the
300 kB budget.

**Not yet deployable-and-demonstrated**, and the gap is measurement rather than code:

- **No performance budget has been measured.** T107's k6 script and T106's timing harness are
  written and parse, but neither has been run — both need the Compose images, which T013 and T027
  record as unbuilt. Until then every number in plan.md's budget table is a target, not a result.
  T218 and T219 are the tasks that close this.
- **T088 required a documented deviation from data-model.md.** The specified
  `UNIQUE (conversation_id, client_message_key)` on `message` **cannot exist** — PostgreSQL refuses a
  unique constraint on a partitioned table that omits the partition key, and including `sent_at`
  would admit the duplicates it exists to prevent, because a retry gets a new timestamp. The
  constraint now lives on `message_dedup`, an un-partitioned table. data-model.md has been corrected
  in place rather than left describing an index nobody can create.
- **Two production bugs were found by tests written for other things**: an unreachable broker took
  the whole API host down (an unhandled `BackgroundService` exception defaults to `StopHost`), and
  `ConsumerHost` disposed a `List` while another thread appended to it. Both are fixed; both were
  invisible until the API host actually consumed a queue.

---

## Phase 5: User Story 3 - Group Conversations (Priority: P2)

**Goal**: Teams converse in named groups with membership that changes over time and a clear,
enforced history-visibility rule.

**Independent Test**: Create a group with three members, post, add a fourth and confirm their
history floor, remove one and confirm they stop receiving immediately.

### Tests for User Story 3 (REQUIRED) ⚠️

- [X] T109 [P] [US3] Unit tests for group name requirement and the rule that a re-add never lowers `visible_from_seq` in `tests/Unit/Domain/MembershipTests.cs` — 12 tests. `Membership` was fully implemented at T059, so these pin behaviour already on the entity rather than drive it into existence, the same way T052 formalised `Employee` invariants — including the load-bearing one, that `Rejoin` takes `Math.Max` of the old and new floor so a re-add never regains history the member was excluded from. Group-name enforcement itself stays covered by `ConversationTests` (T079); this file is `Membership`'s own invariants.
- [X] T110 [P] [US3] Contract tests for the membership endpoints in `tests/Contract/MembershipContractTests.cs` — 5 tests, same shape as `MessagingContractTests`: routing for all three membership operations, and `Member`'s documented properties (openapi.yaml declares no `required` list on it, so this asserts "every documented property is served" rather than a required-property check) including that `employee` nests a full `EmployeeSummary` rather than a bare id.
- [X] T111 [P] [US3] Integration test asserting a removed member immediately loses new messages in `tests/Integration/Conversations/RemovalTests.cs` — 2 tests against the real HTTP surface: a removed member is refused both new history and the conversation detail, indistinguishably from a conversation that never existed (SC-017), while a remaining member keeps reading; and the removal is recorded in the audit log with actor, subject, and outcome (FR-006, spec.md US1 scenario 5). Attachment access is not exercised — attachments do not exist until US5 — so T146 revisits removal for that path.
- [X] T112 [P] [US3] Integration test asserting a newly added member sees history only from their join point in `tests/Integration/Conversations/HistoryVisibilityTests.cs` — 2 tests, covering both `from_join` (hides messages sent before joining, until a new one arrives) and `full` (shows everything) — a test that only covered the default could not tell "respects the rule" from "always hides history".
- [X] T113 [P] [US3] Integration test asserting delivery to a 500-member group stays inside the budget in `tests/Integration/Conversations/LargeGroupDeliveryTests.cs` — asserts accept latency for a 500-member send is not a large multiple of a 3-member send's, rather than an absolute millisecond ceiling: an absolute number is what T107's k6 script and T218 exist to measure, and flakes on whatever hardware runs CI. What this actually proves structurally: `SendMessageHandler` never reads the membership list, so its cost cannot be a function of member count in the first place — see T118's note below.

### Implementation for User Story 3

- [X] T114 [US3] Implement the `AddMember` and `RemoveMember` use cases with membership cache invalidation in `src/InternalChat.Application/Conversations/` — plus `ListMembers`/`IConversationReader.ListMembersAsync` for the member-list projection and `IMembershipCacheInvalidator` (narrow interface over `MembershipCache`'s existing invalidation methods, which T068 built ahead of this task). Both mutations invalidate the conversation's cache and publish `MembershipChanged` inside the same transaction as the row change. `MemberAlreadyActiveException` added and mapped to 409 — the one new response shape US3 needs.
- [X] T115 [US3] Implement the membership endpoints in `src/InternalChat.Api/Endpoints/MembershipEndpoints.cs` — list requires ordinary membership; add and remove require `.RequireConversationMembership(MembershipRole.Admin)`, the same declarative mechanism `GetConversation` uses at a higher floor (`ConversationMembershipRequirement.MinimumRole` was already there, unused, since T066 — this is the task that uses it).
- [X] T116 [US3] Publish `chat.membership.changed.v1` and consume it to move SignalR group assignment immediately in `src/InternalChat.Api/Consumers/MembershipChangeConsumer.cs` — new `membership.fanout` queue (`chat.membership.#`), added to `ChatTopology.Queues` and to `TopologyRoutingTests.ExpectedRouting()`. The event's `change` field is a wire string (`added`/`removed`/`role_changed`), not the `MembershipChangeKind` enum directly — `OutboxEventPublisher` serializes an enum as its numeric value with no converter registered, and adding one would change every event's payload shape, not just this one. The consumer moves each of the affected employee's *local* connections (per-process, like `HubConnectionRegistry` itself) in or out of the group and sends `ConversationCreated`/`MembershipRevoked`; the frontend wires both into `ChatConnection` and `ChatShell` to refresh the list and drop an open, revoked conversation without a reload. `audit.write` and `search.index` are not wired to this event: audit already happens synchronously via `IAuditableRequest` (T072's pattern), and search reindexing on membership change is US6's job (T167).
- [X] T117 [US3] Mention resolution restricted to active members at send time — **already implemented** at `src/InternalChat.Application/Messages/SendMessage.cs`'s `ResolveMentionsAsync`, built during US2 with a comment noting it formalises this exact task. It narrows client-supplied mentions to active members generically, not specifically for direct conversations, so no separate `MentionResolver.cs` was needed; extracting one now would duplicate working, tested logic for no behavioural change.
- [ ] T118 [US3] Route fan-out for conversations above 1,000 members through the Worker — **not built as a separate consumer, and the reason is structural rather than deferred work.** `SendMessageHandler` (T092) never reads the membership list at all: it allocates a sequence, writes the message, and writes one outbox row, independent of member count. Fan-out (`RealtimeFanoutConsumer`, T098) is already off the accept path — it runs from a RabbitMQ consumer, not inline in the request — and delivers with one `Clients.Group(...).SendAsync` call regardless of how many connections are in that group; the per-recipient fan-out is the SignalR Redis backplane's job, not application code's. Building a second consumer that re-delivers the same event for "large" conversations would either duplicate delivery or need a member-count branch somewhere upstream to decide which consumer handles which conversation — complexity with no corresponding gap to close. T113 verifies the property this task was written to protect. Left unchecked rather than marked done, since the file this task names does not exist; revisit only if T218's load test finds a real scaling cost here.
- [X] T119 [P] [US3] Build group creation and member management UI in `src/internalchat-web/src/features/conversations/GroupSettings.tsx` — `CreateGroupForm` (name plus a directory-search picker) and `GroupMembers` (list, admin-only add/remove, mirroring the server's `MembershipRole.Admin` check so the UI does not offer a control that would just come back refused). Wired into `ChatShell` (a "New group" toggle) and `ConversationView` (rendered for `kind === 'group'`).
- [X] T120 [P] [US3] Build mention autocomplete in `src/internalchat-web/src/features/messages/MentionAutocomplete.tsx` — plus pure helpers in `mentionQuery.ts` (split out for the same Fast-Refresh reason `conversations/queryKeys.ts` is split from its components), 11 Vitest tests, and wiring through `Composer.tsx` → `OfflineQueue.enqueue` → `sendMessage` so a picked mention's id actually reaches the server. Candidates are the conversation's own active members (`GroupMembers`' query key, shared), not the whole directory — FR-015 restricts resolution to active members regardless, so offering anyone else would be a UI promise the API does not keep.
- [X] T121 [P] [US3] Display the active history-visibility rule to members in `src/internalchat-web/src/features/conversations/HistoryNotice.tsx` — rendered above the transcript for group conversations in `ConversationView`.
- [X] T122 [US3] Playwright E2E covering quickstart V3 in `tests/e2e/v3-groups.spec.ts` — 3 tests covering scenarios 1, 3, and 4 (create-and-see-without-refreshing, removal, history floor), by roster display name since the UI renders those, never usernames. ⚠️ **Written, type-checked, and enumerated (`playwright test --list`); not executed** — same gap as T106, needing the Compose images T013 and T027 record as unbuilt. Step 4 of quickstart.md's V3 (delivery to "the seeded 500-member group") is not scripted here: no seeder task creates one — T050 seeds 2 small groups — so quickstart.md's reference to a *seeded* 500-member group does not match what exists; T113's integration test covers the underlying property (SC-013) without needing one.

**Checkpoint**: User Stories 1–3 all work independently. Backend: 80 unit, 20 architecture, 39
contract, and 99 integration tests green, the last against real PostgreSQL, Redis, RabbitMQ, MinIO,
and Keycloak. Frontend: 41 Vitest tests, strict typecheck, ESLint clean (0 errors, 0 warnings),
Prettier clean, 111.69 kB gzipped against the 300 kB budget.

One thing carried into Phase 6 rather than fixed here:

- **T122 has not been run**, for the reason T076 and T106 record: it needs the Compose images, which
  T013 and T027 still record as unbuilt. T219 is where it runs.

---

## Phase 6: User Story 4 - Notifications and Unread State (Priority: P2)

**Goal**: Employees are reached when away, without being flooded, with unread state consistent
across every device — and are told plainly when they cannot be reached.

**Independent Test**: Two devices; a background DM produces a notification within 5 seconds;
reading on one device clears the badge on the other; denying permission produces a visible warning.

### Tests for User Story 4 (REQUIRED) ⚠️

- [X] T123 [P] [US4] Unit tests for do-not-disturb evaluation across time zones in `tests/Unit/Domain/DoNotDisturbTests.cs` — 16 tests. The overnight-window case (`start > end`, spanning midnight) is the one a same-day comparison gets backwards; a same-instant-two-zones test proves the zone conversion is load-bearing rather than a no-op, using a synthetic fixed-offset `TimeZoneInfo` rather than a named IANA id — this Windows dev sandbox runs with `InvariantGlobalization` (Directory.Build.props, a deliberate image-size choice), under which `TimeZoneInfo.FindSystemTimeZoneById` cannot resolve IANA names at all; real IANA ids resolve normally on the Linux containers this platform actually deploys to, which read tzdata files directly rather than through ICU.
- [X] T124 [P] [US4] Unit tests asserting read state merges monotonically and never moves backwards in `tests/Unit/Domain/ReadStateTests.cs` — 9 tests, including two out-of-order devices settling on the higher position, which is the actual property FR-036 depends on.
- [X] T125 [P] [US4] Contract tests for the notification endpoints in `tests/Contract/NotificationContractTests.cs` — 10 tests: routing for read-state, mute, preferences, subscription registration, and the VAPID public-key endpoint (below); asserts `dndStart`/`dndEnd` serialize as `"HH:mm"`, not `TimeOnly`'s default format, which a client parsing the contract's own example would fail to read.
- [X] T126 [P] [US4] Integration test asserting unread state clears across a second device in `tests/Integration/Notifications/CrossDeviceReadTests.cs` — 2 tests over a real SignalR connection (not just the HTTP response), plus the out-of-order-report case against the stored row. Needed a manual `OutboxDispatcher.DispatchBatchAsync()` call mid-test: `OutboxDispatcherService` — the loop that drains it — is hosted only by the Worker, which `ApiFactory`-based tests never start.
- [X] T127 [P] [US4] Integration test asserting no notification for an ordinary group message but one for a mention in `tests/Integration/Notifications/MentionOnlyTests.cs` — 3 tests (mention vs. non-mention in one group message, an ordinary message notifying nobody, a direct message always notifying), run through the real `NotificationFanoutConsumer` via `ActivatorUtilities.CreateInstance` — the same pattern `DirectorySyncTests` uses — with only `IPushSender` substituted, since no self-hostable stand-in exists for a browser vendor's push service.
- [X] T128 [P] [US4] Integration test asserting a rejected push subscription is deleted rather than retried in `tests/Integration/Notifications/SubscriptionPruningTests.cs` — 3 tests: a 404/410-shaped result deletes the subscription, a transient failure leaves it in place (the other half of data-model.md's rule — pruning a subscription that might recover is its own bug), and a successful delivery records `last_success_at`.

### Implementation for User Story 4

- [X] T129 [P] [US4] Create `ReadState`, `NotificationPreference`, `DoNotDisturbWindow` in `src/InternalChat.Domain/Notifications/` — **no separate `MuteSetting` type**: per-conversation mute is `Membership.MutedUntil`, already built at T059 for exactly this (FR-037), and a second type would duplicate it. `ReadState` and `NotificationPreference` are plain classes with private constructors (not `Entity<TId>`), the same reasoning `Membership` documents — composite or simple keys with no domain-event-raising identity of their own. `DoNotDisturbWindow.Create` has two overloads, one taking a raw IANA string (resolved via `TimeZoneInfo.FindSystemTimeZoneById`) and one taking an already-resolved `TimeZoneInfo`, so a caller evaluating many windows for one employee (the notification fan-out) can resolve once and reuse it.
- [X] T130 [US4] Add EF configurations and a migration for `read_state`, `notification_preference`, and `push_subscription` in `src/InternalChat.Infrastructure/Persistence/` — **verified against real PostgreSQL**: applied cleanly, all three tables and their FKs present. `push_subscription` is a plain Infrastructure persistence record (`PushSubscriptionRecord`), not a domain type — no invariant attaches to it beyond "the endpoint is unique" and "a rejected one is deleted", both storage concerns, the same reasoning that keeps `MessageDeduplicationRecord` out of the domain. `NotificationPreference.DoNotDisturb` is computed from raw scalar columns rather than mapped as an EF owned type, so a stored row never re-runs `DoNotDisturbWindow`'s validation on read.
- [X] T131 [US4] Implement the `MarkRead` use case with a monotonic merge in `src/InternalChat.Application/Notifications/MarkRead.cs` — publishes `chat.read_state.updated.v1` only when the position actually advanced, constructed directly (not via `Entity.Raise`) the same way `MembershipChanged` is, since `ReadState` is not an `Entity<TId>`.
- [X] T132 [US4] Implement the `UpdatePreferences` use case (plus `GetPreferences`, defaulting rather than 404ing for an employee who never configured anything) in `src/InternalChat.Application/Notifications/UpdatePreferences.cs`.
- [X] T133 [US4] Implement the notification fan-out consumer honouring mute, do-not-disturb, and mention-only rules in `src/InternalChat.Worker/Consumers/NotificationFanoutConsumer.cs` — direct messages always notify; group messages notify only the mentioned (FR-035). Digest suppression is one Redis key per employee (`notify:cooldown:{id}`, shared with `DigestJob` via `NotificationCooldown`): the first eligible message after a quiet period pushes immediately and starts a `DigestAfterMinutes` cooldown, and anything else that would have notified the same employee during it is suppressed rather than queued. Not dispatched through `IUseCaseDispatcher`, matching `RealtimeFanoutConsumer`'s precedent — a multi-recipient side effect, not a single state-mutating use case, and one recipient's failure must not roll back another's success. **Scoped deliberately short of "suppress push while the reader has the exact conversation open"**: no signal for that exists today (the hub tracks connection and group membership, not which conversation a client currently has focused), and adding one is outside this task's literal wording ("honouring mute, do-not-disturb, and mention-only rules") — noted here rather than silently assumed away.
- [X] T134 [US4] Implement `IPushSender` over VAPID web push in `src/InternalChat.Infrastructure/Push/WebPushSender.cs` — `Lib.Net.Http.WebPush` (MIT), added to the dependency licence register, does RFC 8291 encryption and RFC 8292 VAPID signing; this class maps 404/410 (RFC 8030 §7.2 — "gone") to `SubscriptionExpired` and everything else to `TransientFailure`. Validated lazily in its own constructor rather than via `IOptions<T>.ValidateOnStart()`: only the Worker ever resolves `IPushSender`, and `AddInfrastructure()` is shared by both hosts — eager validation would fail the API at startup over configuration (`WebPush:PrivateKey`) it does not carry and does not need.
- [X] T135 [US4] Implement the backlog digest job for returning employees in `src/InternalChat.Worker/Jobs/DigestJob.cs` — a periodic sweep (5 min), scoped to employees who hold at least one push subscription (bounded, not the whole 10,000-employee roster), computing backlog live from `read_state` rather than a separate counter. Fires once a cooldown lapses with unread backlog still outstanding, then restarts the same cooldown — closer to "the backlog is batched, not delivered as individual interruptions" than to a literal single lifetime notification, which the fuzzy `digest_after_minutes` semantics do not specify precisely enough to pin down further; no dedicated test task was assigned for this one.
- [X] T136 [US4] Implement the read-state, preference, and subscription endpoints in `src/InternalChat.Api/Endpoints/NotificationEndpoints.cs` — plus `GET /notifications/vapid-public-key` (openapi.yaml gap: nothing exposed the public key a browser needs to call `PushManager.subscribe`, so no client could ever have registered) and `PUT /conversations/{id}/mute` (openapi.yaml gap: `Membership.MutedUntil` and the read-side `Conversation.mutedUntil` both existed with no path to write either). Both corrected in the contract alongside the endpoint, the same pattern T080/T088 record for earlier gaps. `ConversationReader.SummariseAsync` also now computes real `UnreadCount` and `MutedUntil` from `read_state`/`membership` instead of the hardcoded placeholders US2/US3 left there pending this phase.
- [X] T137 [US4] Broadcast `ReadStateUpdated` to the employee's other devices in `src/InternalChat.Api/Consumers/ReadStateConsumer.cs` — new `readstate.fanout` queue (`chat.read_state.#`), resolving the employee's own local connections the same way `MembershipChangeConsumer` does (subject lookup, then `HubConnectionRegistry`), and recomputing the unread count fresh rather than trusting the event, since the conversation may have moved since it was published.
- [X] T138 [P] [US4] Build the service worker handling push and nothing else in `src/internalchat-web/src/lib/push/service-worker.ts` — typed against a small local interface rather than TypeScript's `WebWorker` lib, which conflicts with the main app's `DOM` lib in the same `tsconfig.app.json` program. Built as its own Rollup entry (`vite.config.ts`, emitted at the stable root path `service-worker.js`) rather than via `new Worker(new URL(...))`, which is Vite's automatic asset-URL handling for the `Worker`/`SharedWorker` constructors specifically — `ServiceWorkerContainer.register` isn't one of them, and the URL would otherwise resolve at runtime to a file nothing ever built. **Verified**: `npm run build` emits `dist/service-worker.js` (0.36 kB gzipped) as a standalone bundle.
- [X] T139 [P] [US4] Build notification capability detection and the plain-language warning required by FR-040 in `src/internalchat-web/src/features/notifications/NotificationCapability.tsx` — plus `lib/push/pushSubscription.ts` (detection + subscribe flow, split out for the Fast-Refresh reason `conversations/queryKeys.ts` is split from its components) and a new `GET /notifications/vapid-public-key` endpoint it calls (see T136). Renders nothing when the server already reports a live subscription; otherwise shows one of three messages — unsupported, permission denied, or "off, here's a button" — since only the browser can tell those apart, and offers to enable only in the one case that is actually actionable. Replaces `App.tsx`'s old hardcoded warning.
- [X] T140 [P] [US4] Build unread badges and the do-not-disturb settings UI in `src/internalchat-web/src/features/notifications/` — badges needed no new UI: `ConversationList.tsx` (T100) already rendered `conversation.unreadCount`, which was simply always zero until T136 made it real. What was missing and is added now: `NotificationSettings.tsx` (DND window via native `<input type="time">`, which produces the exact `"HH:mm"` string the API expects with no format to get wrong; digest threshold), a mute toggle in `ConversationView.tsx` (FR-037's other half — muting is local and optimistic, not shared state, so no cache coordination is needed), and the `PUT /conversations/{id}/read-state` call itself (`ConversationView.tsx`, on every change to the highest visible message) — without which nothing would ever have advanced a read position for the badges to react to.
- [X] T141 [US4] Playwright E2E covering quickstart V4 in `tests/e2e/v4-notifications.spec.ts` — 3 tests: the FR-040 warning for an employee with no subscription, cross-device unread clearing over a real second SignalR connection, and DND settings surviving a reload. ⚠️ **Written, type-checked, and enumerated; not executed** — same Compose-image gap as T106/T122. **Deliberately not scripted**: quickstart V4 steps 1, 2, and the interruption half of step 4, all of which need a real push notification to actually arrive at the OS level — Playwright has no API to observe that, and faking it by calling the service worker directly would test the test rather than the platform. T219 is where those run by hand.

**Checkpoint**: The platform is now a reliable channel rather than somewhere to check. Backend: 105
unit, 20 architecture, 50 contract, and 108 integration tests green, the last against real
PostgreSQL, Redis, RabbitMQ, MinIO, and Keycloak. Frontend: 42 Vitest tests, strict typecheck,
ESLint clean, Prettier clean, production build verified (main bundle ~113 kB gzipped against the
300 kB budget, plus the standalone 0.36 kB service worker).

One thing carried into Phase 7 rather than fixed here:

- **T141 has not been run**, for the reason T076, T106, and T122 record: it needs the Compose
  images, which T013 and T027 still record as unbuilt. T219 is where it runs — including, by hand,
  the push-delivery scenarios no automated test in this suite can observe.

---

## Phase 7: User Story 5 - Image Sharing (Priority: P2)

**Goal**: Screenshots and photos post inline, are scanned before anyone can retrieve them, and obey
the same access rules as the conversation.

**Independent Test**: Post an image to a group; every member sees it inline; a non-member with the
content URL is refused; EICAR never becomes retrievable.

### Tests for User Story 5 (REQUIRED) ⚠️

- [X] T142 [P] [US5] Unit tests for per-kind size and content-type constraints in `tests/Unit/Domain/FileConstraintsTests.cs` — 37 tests, **observed failing first** (the namespace did not exist). Cover per-kind ceilings at the boundary, the disjoint allow-lists, RFC 9110 media-type comparison (case and parameters), `image/svg+xml` refused as stored XSS rather than as a scan problem, and the limit being readable without provoking a violation, which is the other half of FR-023
- [X] T143 [P] [US5] Contract tests for the attachment endpoints in `tests/Contract/AttachmentContractTests.cs` — 10 tests, **observed failing first**: the 3 routing assertions failed against unmapped routes while the 7 DTO-shape assertions already passed, then all 10 passed once the endpoints landed. The sharpest one asserts `contentUrl` is a *path*, not a presigned URL: if it ever became one the feature would still work — images would render — and FR-025 would be silently broken, so nothing about the system behaving correctly would reveal it
- [X] T144 [P] [US5] Integration test asserting a non-member is refused the content URL in `tests/Integration/Attachments/AccessControlTests.cs` — 5 tests against the real stack. The non-member is handed the *exact* URL, because FR-025 is about a leaked address being worthless, not an unguessable one. Also asserts the refusal is byte-identical to one for an id that never existed (SC-017, trace id excluded — it must differ), that metadata is gated too (a file name and size describe conversation content), and that access ends the moment membership does
- [X] T145 [P] [US5] Integration test asserting the EICAR test file never becomes retrievable in `tests/Integration/Attachments/MalwareScanTests.cs` — 5 tests against a **real ClamAV container**, not a stub: a stubbed scanner would assert the consumer believes what it is told, which says nothing about whether malware is caught. One test asserts ClamAV detects EICAR *and* clears an ordinary file, so a scanner silently reporting success cannot make the rest pass vacuously. Also covers purge from both buckets, clean promotion with the measured size, and an infected verdict surviving redelivery. ClamAV is started per-class rather than in `StackFixture` — a ~2-minute cold start should not be paid by every integration run. The EICAR string is assembled at runtime so this source file is not itself a detection on a developer machine
- [X] T146 [P] [US5] Integration test asserting deleting a message stops the attachment being served in `tests/Integration/Attachments/DeletionTests.cs` — 4 tests. Asserts the stop applies to *every* member, not just the author, and — deliberately — that the row and the object both **survive**: FR-027 is about reachability, and purging on delete would satisfy the letter while breaking retention and audit and making the operation irreversible in a way nothing asked for
- [X] T147 [P] [US5] Integration test asserting an interrupted upload leaves no partial or duplicate attachment in `tests/Integration/Attachments/InterruptedUploadTests.cs` — 6 tests. 'No partial' holds by construction (a row starts `pending`, nothing is served from `pending`, only the scan consumer can advance it); 'no duplicate' is the retry case — two independent rows with distinct object keys, so a retry's bytes cannot land on top of the abandoned upload's. Also covers the three ways a send can name an attachment it may not have (someone else's, another conversation's, one that never existed), each refused identically and each rolling back the **whole send** — a message persisting without the image its sender attached is the failure FR-026 exists to prevent

### Implementation for User Story 5

- [X] T148 [P] [US5] Create `Attachment`, `ScanVerdict`, and `FileConstraints` in `src/InternalChat.Domain/Attachments/` — plus `tests/Unit/Domain/AttachmentTests.cs` (20 tests) for the scan state machine, which the Domain 90% floor requires and which no other task covers. `failed` is kept distinct from `infected` deliberately: collapsing them would either accuse an employee of malware because ClamAV restarted, or serve unscanned bytes. `RetryScan` accepts only `failed`, so a retry can never launder an infected object clean. **Contract gap found**: `openapi.yaml` requires `fileName` on `RequestUploadRequest` and returns it on `Attachment`, but the `attachment` table in `data-model.md` has no `file_name` column — added to the entity and the schema from the contract
- [X] T149 [US5] Add the EF configuration and migration for `attachment` with the denormalized `conversation_id` in `src/InternalChat.Infrastructure/Persistence/` — **Verified against real PostgreSQL**: all 7 migrations applied to a fresh database, re-apply is a no-op, and each of the 5 CHECK constraints was proved to fire by 11 savepointed insert cases (limits accepted at the boundary, refused one over; a duration-less video, a timed image, a half-set message reference, a `clean` row with no `scanned_at`, and a poster on an image all rejected). **Three departures from data-model.md, all recorded in the configuration's remarks**: (1) *no FK on `message_id`* — `message` is partitioned and FR-052 expires history by dropping partitions, which an inbound FK makes PostgreSQL refuse; (2) *`message_sent_at` added* — `message`'s PK is `(id, sent_at)`, so an id alone cannot locate a row, and FR-027's deleted-check is on the hot path for every image render; (3) *`file_name` added* — required by `openapi.yaml`, absent from the table
- [X] T150 [US5] Implement `IObjectStore` over MinIO with quarantine and clean buckets in `src/InternalChat.Infrastructure/Storage/MinioObjectStore.cs` — plus `MinioOptions` and singleton client registration. The two buckets are the security boundary rather than a status column: an unscanned object is unreachable *by construction*, not by the serving path remembering to ask. Constructor refuses a configuration where both bucket names are equal, which would silently collapse that boundary. Promotion is a server-side copy then delete (a 500 MB video must not cross the network twice through the Worker), and the copy is acknowledged before the delete — a duplicate in quarantine is untidy, a lost object is not recoverable. The presigned PUT binds `Content-Type` and `Content-Length` into the signature as defence in depth; the authoritative check stays the re-measure at promotion
- [X] T151 [US5] Implement the `RequestUpload` use case validating kind, type, size, and duration before any bytes transfer in `src/InternalChat.Application/Attachments/RequestUpload.cs` — plus `IAttachmentRepository` and its EF implementation. Validation runs before the row is written and long before a ticket exists, so a client declaring a 600 MB video learns the limit without transferring a byte. The capacity gate refuses at 95% rather than 100% (a declared size is a claim, and concurrent uploads race the same free space) and refuses *only this path*, so text messaging keeps working — the second half of FR-028, and why the check is not a global health gate. Nothing is published: a reserved attachment carries no bytes, belongs to no message, and may never be redeemed
- [X] T152 [US5] Implement the ClamAV scan consumer performing promotion and emitting `AttachmentReady` in `src/InternalChat.Worker/Consumers/AttachmentScanConsumer.cs` — plus `IMalwareScanner` and `ClamAvScanner` (hand-rolled INSTREAM; `SCAN` would require sharing a writable volume with the scanner container). **Event names corrected against contracts/messaging.md**: the first draft invented `chat.attachment.ready.v1`/`chat.attachment.infected.v1`; the contract declares `chat.attachment.uploaded.v1` and a single `chat.attachment.scanned.v1` carrying the verdict, with `AttachmentReady` being the *SignalR* event. Realigned. The upload event is raised by `AttachTo`, because the client PUTs bytes straight to MinIO and a send naming the attachment is the only signal the server ever gets that the object exists. **Order is the requirement**: scan → re-measure → promote → record `clean`; recording first would publish a retrievable row whose object is not yet there, promoting first would put unscanned bytes in the bucket the serving path reads. The declared size is re-checked against what actually arrived, so a file that lied past the upload gate is refused even when clean. A failed scan is recorded and acknowledged, never thrown — throwing would burn the capped retries on a scanner outage and strand the row at `pending` in the DLQ
- [X] T153 [US5] Implement the `AuthorizeDownload` use case and the `X-Accel-Redirect` handoff so bytes never pass through .NET in `src/InternalChat.Api/Endpoints/AttachmentEndpoints.cs` — plus `GetAttachmentMetadata`, `AttachmentMapper`, and `AttachmentDeliveryOptions`. **The two retrieval routes deliberately carry no membership filter**: they name only an attachment id, so the conversation is a property of the row and applying the filter would mean trusting a caller-supplied conversation id — the one input that must not be trusted here. The check happens inside the handler against the row's own `ConversationId`. Every refusal is the same refusal (missing, non-member, unscanned, tombstoned) so enumeration cannot distinguish a real id from a fake one (SC-017); the sole exception is `451 infected`, safe only because it is reached *after* membership is confirmed. `AttachmentMapper` is the single line in the codebase that decides what a client is told to fetch, and it emits a relative route — never a presigned URL
- [X] T154 [US5] Configure the internal-only attachment location with range support in `deploy/nginx/nginx.conf` — the `internal` location already existed from T051; this task found and fixed a **conflict with US5**: it forced `Content-Disposition: attachment` on every response, which defeats FR-021's inline previews and FR-022's play-in-place, and would have emitted a *second* conflicting header alongside the one the API sets (X-Accel-Redirect preserves upstream headers). The header is now left to the API. That defence is not lost, only relocated: the scriptable types it guarded against — `image/svg+xml`, `text/html` — are refused by name at upload, so nothing scriptable can reach this location. The allow-list plus `nosniff` is the control. **Config validated with `nginx -t`** against the pinned 1.30.4-alpine image
- [X] T155 [US5] Implement the storage capacity monitor and administrator alert in `src/InternalChat.Worker/Jobs/StorageCapacityJob.cs` — warns at 75%, escalates at 90%, and reports separately once uploads are actually being refused at 95%. The thresholds sit *below* `RequestUploadHandler.CapacityRefusalThreshold` deliberately: an alert firing at the refusal point would announce an outage rather than let anyone prevent one, and nobody provisions storage in the seconds between. Emitted as log events at severities the T046 observability stack already routes, rather than inventing a second notification channel nobody has subscribed to
- [X] T156 [P] [US5] Build image upload with progress and clipboard paste in `src/internalchat-web/src/features/attachments/ImageUpload.tsx` — plus `fileConstraints.ts` (26 vitest tests) and `paste.ts`. Bytes go straight to MinIO via the presigned ticket, never through the API (research.md D7). Progress uses `XMLHttpRequest` rather than `fetch`, which still cannot report upload progress in any shipping browser — the one place the older API earns its place, and FR-026 requires progress. The limits are mirrored from the server's `FileConstraints`: duplication on purpose, because a client that learned the limit only from a 413 would have to attempt the upload to discover it, which is exactly what FR-023 forbids. A failed upload stays in the list rather than disappearing — silently dropping it is how someone ends up believing they shared a file
- [X] T157 [P] [US5] Build the inline preview and lightbox in `src/internalchat-web/src/features/attachments/ImagePreview.tsx` — renders four states, and the `pending` one is why the component exists: FR-024 makes the unscanned window unavoidable, so a preview that only knew 'image' and 'broken image' would show a broken image on every send. Polls only while something is pending, and treats polled results as an **overlay on props** rather than a copy — the copy-and-resync shape renders stale data for a frame and lets the parent and child disagree. `infected` is stated plainly, because silence looks like a failed upload to someone expecting the file
- [X] T158 [US5] Playwright E2E covering quickstart V5 image scenarios in `tests/e2e/v5-images.spec.ts` — 5 tests covering steps 1, 2, 3, 4, and 6; step 5 (progressive playback) belongs to US7 and is scripted in `v7-video.spec.ts` beside the range-request work it depends on. **⚠️ Written and type-checked, not executed** — same gap as T106/T122/T141 (Compose images unbuilt, T013/T027); T219 is the manual run. The oversized-video test asserts the *absence of any network request*, since FR-023's claim is that the refusal precedes the transfer — an error message alone could have appeared after one. The leaked-URL test is the one thing only a browser can prove: that the address is worthless in a real session, cookies and service worker included

**Checkpoint**: Attachments work with the access model intact.

---

## Phase 8: User Story 6 - Search Across Messages and Files (Priority: P3)

**Goal**: Employees find messages across their own conversations quickly, and never learn that
content exists in conversations they cannot access.

**Independent Test**: Search a phrase and get ranked results only from your conversations; search a
phrase that exists only elsewhere and get nothing, with nothing in timing or counts revealing it.

### Tests for User Story 6 (REQUIRED) ⚠️

- [X] T159 [P] [US6] Unit tests for query parsing and filter validation in `tests/Unit/Application/SearchQueryTests.cs` — 21 tests. The validator is the only part of search a unit test can assert honestly; ranking, scoping, and diacritic folding are properties of PostgreSQL and a test against a fake would be asserting the fake. A reversed date range is **refused rather than swapped**, and an over-limit page is **refused rather than clamped** — both for the same reason as FR-033: silently giving a caller something other than what they asked for makes them draw a wrong conclusion with no signal
- [X] T160 [P] [US6] Contract tests for the search endpoint in `tests/Contract/SearchContractTests.cs` — 5 tests, plus a `QueryParametersFor` reader added to `OpenApiContract`. The load-bearing assertions are the ones about fields whose absence fails *silently*: `truncated` (without it FR-033 is unimplementable, and a truncated search is externally identical to a complete one) and `seq` (without it a result can be identified but not navigated to, so FR-031 dies quietly). Also asserts every documented filter is actually bound — an unbound filter returns *more* results than asked for, with no indication
- [X] T161 [P] [US6] Integration test asserting no results and indistinguishable timing for content in non-member conversations in `tests/Integration/Search/AccessScopingTests.cs` — 7 tests. Includes the control case (a member of the *other* conversation does find the phrase), without which an empty result set would be equally consistent with the phrase never having been indexed. The timing assertion's limits are stated in the file rather than implied: a single-machine comparison cannot prove the absence of a side channel, so the bound is loose on purpose — it catches the membership filter being moved to *after* ranking, which would be an order of magnitude, and does not pretend to catch a microsecond. The structural argument (the filter is inside the query) is what makes the property true; the test stops it being quietly lost
- [X] T162 [P] [US6] Integration test asserting edits, deletions, and membership removal are reflected in results in `tests/Integration/Search/ReindexTests.cs` — 5 tests. The edit case asserts **both** halves (new text findable, old text not), because a trigger that concatenated rather than replaced would pass the first and fail the second. Also asserts the tombstone *survives* the delete that made it unsearchable — an index that achieved FR-032 by removing the row would break the sequence continuity every reconnect depends on

### Implementation for User Story 6

- [X] T163 [US6] Add the `unaccent` extension, the generated `body_tsv` column, and the GIN index in `src/InternalChat.Infrastructure/Persistence/Migrations/` — **hit the known PostgreSQL trap**: `unaccent()` is `STABLE`, and a generated column requires `IMMUTABLE`, so PostgreSQL refuses it outright. Solved with an `internalchat_immutable_unaccent` wrapper, which is an explicit acceptance of the risk rather than a trick: *if the unaccent dictionary is ever changed, every stored vector becomes subtly wrong and must be rewritten*. Accepted because the dictionary is the stock one, pinned by the image tag. Chose the generated column over research.md D9's trigger because a row-level trigger sits on the hottest insert path at 100 msg/s. **Verified against real PostgreSQL, 6 checks**: the column populates, `ke hoach` (unmarked) matches `kế hoạch`, the marked form matches too, English terms match, a tombstone's vector stops matching (FR-032), and 17 GIN indexes exist (parent + 16 partitions)
- [X] T164 [US6] Implement `ISearchIndex` over PostgreSQL full-text search with a membership pre-filter applied before ranking in `src/InternalChat.Infrastructure/Search/PostgresSearchIndex.cs` — **found and fixed a real defect while testing**: the first version opened its own connection from `Database.GetConnectionString()`, which Npgsql returns with the **password redacted**, so every search failed SCRAM authentication at runtime while looking correct in code. Now uses the context's own connection. `websearch_to_tsquery` rather than `to_tsquery` — the latter raises a syntax error on a bare apostrophe, turning a search for "it's" into a 500. `IndexMessageAsync`/`RemoveMessageAsync` are **intentionally empty**: the generated column means indexing happens inside the write, so FR-032 holds transactionally rather than eventually. They stay on the interface as the OpenSearch seam
- [X] T165 [US6] Implement the `SearchMessages` use case with filters and an explicit truncation flag in `src/InternalChat.Application/Search/SearchMessages.cs` — plus `IConversationReader.ListAccessibleIdsAsync`, deliberately separate from `ListForEmployeeAsync` (which computes unread counts and previews a search scope has no use for) and deliberately **unpaged**, since paging the scope would silently narrow what a search covers. A named `conversationId` is **intersected** with the accessible set, never substituted for it, so naming a conversation you are not in returns nothing rather than access. The 800 ms statement timeout sits below SC-007's 1-second budget so `truncated` is reachable at all
- [X] T166 [US6] Implement the search endpoint in `src/InternalChat.Api/Endpoints/SearchEndpoints.cs` — no route-level membership filter, and that is not an omission: a search spans conversations by definition, so there is no conversation in the path for a filter to check; the scope is computed inside the handler. **Knowing divergence from `openapi.yaml`**, recorded rather than silent: the contract nests a full `Message` in each result, and `SearchResultResponse` carries `messageId` + `seq` + `highlight` instead — inlining a hundred message bodies into a response whose purpose is helping someone *choose* one is the wrong trade, and the client fetches context on jump. `to=2026-03-01` is widened to the end of that day, since treating a `format: date` as midnight silently excludes everything sent on the day the person asked for
- [ ] T167 [US6] Implement the search index consumer handling edits, deletions, and membership changes — **not built, and the reason is structural rather than deferred work**, exactly as with T118. The consumer would have nothing to do. `body_tsv` is a `GENERATED ALWAYS ... STORED` column, so an edit's new text is searchable in the same transaction that wrote it and a delete's cleared body empties the vector in the same transaction — FR-032 holds *transactionally*, with no window in which the index disagrees with the row, which is strictly stronger than what a consumer could offer. Membership removal needs no indexing either: the search scope is recomputed from the database on every request, so a removed member's next search simply no longer includes that conversation. Building a consumer to re-index rows PostgreSQL has already re-indexed would add a queue, a retry path, and a DLQ to guard a window that does not exist. T162 verifies all three properties against the real database. Left unchecked rather than marked done, since the file this task names does not exist; revisit only if D9's OpenSearch trigger fires, where an external index would genuinely need one
- [X] T168 [P] [US6] Build the search UI with filters and jump-to-context in `src/internalchat-web/src/features/search/` — `SearchPanel.tsx` plus `highlight.ts` (7 vitest tests). **The project's ESLint config blocks `dangerouslySetInnerHTML` without "a reviewed sanitizer allow-list (constitution: Security Requirements / Application Controls)"** — `highlight.ts` is that sanitizer, kept in its own file so the thing under review is 30 lines. It escapes everything first and then restores exactly one element, `<mark>`, which takes no attributes, no URL and no behaviour. `ts_headline` does *not* escape the body it wraps, so rendering the snippet raw would be stored XSS delivered to the person searching for the message. A generation counter discards out-of-order responses (a slow "de" landing after a fast "deployment"), and the search is debounced because the server's fixed-window limit would otherwise 429 someone for typing normally. Rendering `truncated` is a correctness requirement here, not polish: it is the only field whose omission makes the UI lie
- [X] T169 [US6] Seed a 125-million-message corpus and verify the search budget — this is the D9 decision trigger for OpenSearch — in `tests/Load/search.js` — **script written and syntax-checked; NOT executed, and the corpus NOT seeded.** Seeding 125 M rows needs hours and hundreds of GB, and the Compose images are still unbuilt (T013, T027). This is the same recorded gap as T106/T122/T219, and it matters more than the others: until it runs, D9's choice of PostgreSQL over OpenSearch is **unverified**, not confirmed. The script guards against being misread — `setup()` probes the corpus size and *fails* below 1 M messages rather than reporting a flattering p95 against the dev dataset, because the likeliest failure here is someone recording a 12 ms result as proof the budget is met. Thresholds are per query shape, so an aggregate cannot pass while every Vietnamese query is over budget; the no-match shape has its own threshold because a search matching nothing being *slower* is both a performance problem and the T161 timing channel
- [X] T170 [US6] Playwright E2E covering quickstart V6 in `tests/e2e/v6-search.spec.ts` — 6 tests. **⚠️ Written and type-checked, not executed** (same gap as T158). Step 1's "within 1 second" is deliberately left to `tests/Load/search.js`: a browser timing includes React rendering and would report a different number against a different bar. What the browser adds is what k6 cannot see — that a non-member's search is *indistinguishable from empty in the UI*, that jump-to-context lands on a message pushed out of view, and that the truncation notice actually renders. The truncation test **skips** without a seeded corpus rather than stubbing the response: forcing the flag would assert that React renders a prop, which is not the claim worth testing

**Checkpoint**: History becomes an asset rather than a liability.

---

## Phase 9: User Story 7 - Video File Sharing (Priority: P3)

**Goal**: Short recordings play in place without downloading the whole file, under the same access
rules and without transcoding on the application host.

**Independent Test**: Upload a valid MP4 and confirm playback starts before full download; upload a
600 MB file and confirm rejection before any bytes transfer.

### Tests for User Story 7 (REQUIRED) ⚠️

- [X] T171 [P] [US7] Unit tests for video duration, size, and codec allow-list in `tests/Unit/Domain/VideoConstraintsTests.cs` — 24 tests. Makes the FR-023 boundary explicit: size and content type are refused *before* upload because they are declared, but a codec lives in the bitstream, so an honest `video/mp4` carrying H.265 passes every pre-upload gate and can only be caught after transfer. A silent video is accepted (a screen recording with no audio is the majority case); an unplayable *audio* codec is refused rather than stripped, since a video that plays silently is worse than a clear refusal
- [X] T172 [P] [US7] Integration test asserting a range request returns 206 with correct bytes in `tests/Integration/Attachments/RangeRequestTests.cs` — 6 tests covering a bounded range, an open-ended one (`bytes=4000-`, what a player sends after seeking), and a suffix one (`bytes=-128`, how a player finds a trailing MP4 index). **Scope stated in the file**: production serves ranges through nginx, and the integration stack has no proxy, so these exercise the fallback streaming path — they verify authorization, object selection and range handling, not the proxy, which is T176's `nginx -t` and the manual run. Also asserts a non-member is refused *for a ranged request too*, so a range cannot become a path that reaches bytes without the FR-025 check
- [X] T173 [P] [US7] Integration test asserting an oversized or unsupported video is rejected before upload with the limit stated in `tests/Integration/Attachments/VideoRejectionTests.cs` — 10 tests. **"Before upload" is asserted as the absence of a stored row**, not merely as a 413: an endpoint that reserved the attachment and then failed would return the same code while leaving a live ticket behind, so the client could still transfer 600 MB to a location already refused. Includes the accepting boundary (a video *at* the limit succeeds), without which every rejection test would pass against an implementation that refuses all video

### Implementation for User Story 7

- [X] T174 [US7] Extend `FileConstraints` with video duration and the browser-playable codec allow-list in `src/InternalChat.Domain/Attachments/FileConstraints.cs` — added `ValidateProbedVideo`, `AllowedVideoCodecs`, `AllowedAudioCodecs`, and `UnsupportedCodecException`. Kept as a *separate* refusal from `UnsupportedContentTypeException` because it is a different failure reached at a different time: the content type was honest — the file really is an MP4 — and only the bitstream revealed nothing can play it. Merging them would produce a 415 telling someone their MP4 is not an accepted type, which is both wrong and unactionable. The measured duration is re-checked here, catching a client that declared 60 seconds and uploaded an hour
- [X] T175 [US7] Add poster-frame extraction and `moov` atom relocation to the scan consumer in `src/InternalChat.Worker/Consumers/AttachmentScanConsumer.cs` — plus `IVideoProcessor` and `FfmpegVideoProcessor` (ffmpeg was already in the Worker image from T013). **Not a transcoder, deliberately**: D8 rejected transcoding outright, and this is scoped to the two operations that cannot be moved to the uploader's machine — reading what actually arrived, and relocating an index with `-c copy` (an I/O cost, not a CPU one). The remux writes back to **quarantine**, not the clean bucket, because those bytes are still unpromoted and writing them to the clean bucket would break the FR-024 ordering. Codec failure is a refusal; a missing poster or failed remux is a **degradation** — withholding a file over a missing thumbnail is worse than the cosmetic loss. Temporary files rather than pipes, because `ffprobe` must seek to read the `moov` atom and `+faststart` writes the output twice; piping would report every file as having no index, which is the value being measured
- [X] T176 [US7] Enable range-request support through the internal redirect in `deploy/nginx/nginx.conf` — already present from T051 (`proxy_set_header Range`/`If-Range`, `proxy_buffering off`), and **verified rather than assumed**: config validated with `nginx -t` against the pinned 1.30.4-alpine image during T154. The `Content-Disposition: attachment` conflict that would have defeated in-place playback was found and fixed under T154
- [X] T177 [P] [US7] Build the video player with poster and seek in `src/internalchat-web/src/features/attachments/VideoPlayer.tsx` — `ImagePreview` now delegates to it rather than carrying a second copy. **`preload="metadata"` is the line that matters**: the default `auto` tells the browser to buffer the whole file on mount, so a conversation with six videos in view starts six 400 MB downloads and FR-022 is defeated by the markup rather than the transport. The unplayable-format path offers a download link to the same authorized route — a route, not a capability, so it takes nothing away from FR-025
- [X] T178 [US7] Playwright E2E covering quickstart V7 video scenarios in `tests/e2e/v7-video.spec.ts` — 5 tests, including V5 step 5 which was deferred here from T158. **⚠️ Written and type-checked, not executed** (same gap as T158/T170). Asserts progressive playback as *network behaviour* — the player reaches `readyState >= 2` and the server answered 206 — rather than as a timing, because a timing assertion on a fast local stack passes for the wrong reason. The poster test accepts "a poster **or** a rendered first frame", since a clip shorter than the one-second seek point legitimately has none and requiring it would fail on a valid video

**Checkpoint**: All non-meeting functionality is complete and deployable on a single host.

---

## Phase 10: User Story 8 - Video Meetings (Priority: P4)

**Goal**: Up to 25 participants meet from a conversation, with the platform-wide ceiling enforced
by refusal rather than by degradation.

**Independent Test**: Three participants join from separate machines with two-way audio and video;
a non-member is refused a join token; at the ceiling, new meetings are refused while running
meetings are unaffected.

> **Gate**: do not start this phase until the application host meets its messaging budgets. Meeting
> work must never be able to mask a messaging regression.

### Tests for User Story 8 (REQUIRED) ⚠️

- [X] T179 [P] [US8] Unit tests for the 25-participant cap, the 1,250 platform ceiling, and refusal behaviour in `tests/Unit/Domain/MeetingCapacityTests.cs` — 12 tests. **Only one of the two ceilings is testable here**, and the split is recorded rather than glossed: the 25-cap is a property of one room so the entity owns it; the 1,250 platform ceiling spans every room and lives in Redis, so it needs the real store. The most valuable test is the idempotent join — LiveKit delivers at least once and a reconnecting client re-fires `participant_joined`, which counted twice would walk a room towards its cap until it refused people who were not there
- [X] T180 [P] [US8] Contract tests for the meeting endpoints in `tests/Contract/MeetingContractTests.cs` — 8 tests. Two are **negative and deliberate**: the LiveKit webhook must be *mapped but absent from the contract* (it is authenticated by signature, so publishing it would put an endpoint in the client contract that refuses every client), and it must sit *outside* `/api/v1` (a third-party callback inside the versioned surface would force a future v2 to either move LiveKit's configured URL or keep a v1 path alive for it)
- [X] T181 [P] [US8] Integration test asserting a non-member is refused a join token in `tests/Integration/Meetings/TokenAuthorizationTests.cs` — 6 tests. This endpoint is the **entire** meeting security boundary: the media server trusts its token completely and runs no membership check, so a wrongly-minted token is never caught downstream — the person is simply in the meeting. Covers non-member, ended meeting, non-existent meeting, and loss of membership mid-session, each refused identically so none doubles as an id oracle
- [X] T182 [P] [US8] Integration test asserting that with the media host unreachable, meetings report unavailable while messaging, attachments, search, and notifications keep working in `tests/Integration/Meetings/MediaHostDegradationTests.cs` — 7 tests. **This is the test that justifies the two-host architecture**: constitution v1.2.0 permits a second machine only in exchange for the platform staying fully functional without it, so most assertions are about the *other* features. The media host is unreachable **by default** in the integration stack (`ApiFactory` points at a closed port), which makes this the cheap continuously-run test rather than one needing an SFU. Includes the readiness probe staying green — a probe that failed over a second-host outage would let an orchestrator turn a degraded feature into a total outage, the exact inversion of the split's purpose

### Implementation for User Story 8

- [X] T183 [P] [US8] Create `Meeting`, `Participation`, and `ShareSession` in `src/InternalChat.Domain/Meetings/` — the meeting id **is** the LiveKit room name (one identifier, so a webhook naming a room resolves to a meeting or to nothing, never to the wrong one). `End` takes no actor because FR-047 means nobody ends a meeting — the room does when it empties. Rejoining resumes the same participation with `JoinedAt` unmoved, since FR-051's honest duration for someone who dropped and reconnected spans the whole time. `ShareSession` records *why* sharing stopped, because FR-050 requires the rule to be **visible** and 'sharing stopped' cannot distinguish 'you stopped it' from 'someone took over'
- [X] T184 [US8] Add EF configurations and a migration for `meeting` and `participation` in `src/InternalChat.Infrastructure/Persistence/` — participations mapped as an **owned collection**, so they cannot be loaded or written independently of their meeting. Nothing cascades on delete: an employee who leaves keeps their participation rows, because an audit record with the participants removed is not an audit record. **Verified against real PostgreSQL, 9 savepointed cases**: peak 25 accepted / 26 refused, end-before-start refused, duplicate participation refused by the composite PK, left-before-joined refused, negative share seconds refused, and the active-meeting index confirmed partial
- [X] T185 [US8] Create the media host stack with LiveKit and TURN in `deploy/docker-compose.media.yml` — plus `deploy/livekit/livekit.yaml` and `deploy/.env.media.example`. **Validated with `docker compose config`.** Host networking is not laziness: an SFU needs ~10,000 contiguous UDP ports and publishing them through Docker's userland proxy does not work in practice — which is precisely why this service needs a host of its own. TURN is **TLS-only on 5349**, because a relay that works only on networks already permitting UDP relays nothing, and the networks that need it are the ones blocking UDP. **Also fixed `.gitignore`**: `deploy/.env.*` matched the new example file and `!deploy/.env.example` un-ignores only that exact name, so the example nobody can deploy without was silently untracked
- [X] T186 [US8] Implement `IMeetingTokenIssuer` over LiveKit in `src/InternalChat.Infrastructure/Meetings/LiveKitTokenIssuer.cs` — hand-built HS256 JWT rather than the SDK, the same Principle VIII call as the hand-rolled ClamAV INSTREAM client. `roomCreate`, `roomAdmin`, and `roomList` are explicitly **denied** on join tokens: admin would let one participant remove another, bypassing every FR-051 audit record. The availability probe is cached in an **immutable reference replaced wholesale** rather than a mutable tuple — a `(DateTimeOffset, bool)` is wider than a machine word and can tear, pairing one probe's timestamp with another's result and pinning availability to a stale value
- [X] T187 [US8] Implement the `StartMeeting` use case with the Redis platform-wide capacity guard in `src/InternalChat.Application/Meetings/StartMeeting.cs` — plus `IMeetingCapacityGuard` and `RedisMeetingCapacityGuard`. One live meeting per conversation, and a second attempt **joins** rather than competing — two people clicking start must not produce two rooms with half the participants in each. The guard **fails open** on a Redis outage, which is correct precisely because data-model.md says this is a capacity guard and *not* an authorization decision; Principle VII's cache prohibition does not apply, and the per-room cap still holds from PostgreSQL. Capacity is checked but never *reserved*: a reservation would need compensating release on every failure path including the client simply never connecting
- [X] T188 [US8] Implement the `IssueJoinToken` use case performing the membership check that IS the meeting access control in `src/InternalChat.Application/Meetings/IssueJoinToken.cs` — the conversation comes from the **meeting row**, never from the request, so a caller cannot nominate a conversation they happen to belong to. The display name comes from the directory, never from the client: a client-supplied name would let anyone appear on a tile under a colleague's, which the media server cannot detect. **This token really is a bearer capability**, unlike an attachment URL — the media server cannot re-check it — so the only control is a 5-minute life, matched to FR-030's revocation bound. The participation row is deliberately *not* written here: asking for a token is not joining, and the webhook is the only signal that means someone is actually in the room
- [X] T189 [US8] Implement the signature-verified LiveKit webhook receiver driving meeting lifecycle in `src/InternalChat.Api/Endpoints/MeetingWebhookEndpoints.cs` — plus the `ApplyMediaSignal` use case. **The architecture gate caught this endpoint** and refused the build until it was added to `JustifiedAnonymousRoutes` with a reason — the gate working exactly as designed. Verification checks **two** things: the JWT's own HMAC *and* that its `sha256` claim matches the raw body, both in fixed time. Checking only the token would accept a valid one replayed over a different body, which is what would let someone fabricate meeting attendance in the FR-051 audit record. Always returns 200 for events it ignores, since LiveKit retries a non-2xx and retrying an unhandled event is a loop ending in its dead letters
- [X] T190 [US8] Implement the meeting endpoints including the 503 with `retryAfterSeconds` at capacity in `src/InternalChat.Api/Endpoints/MeetingEndpoints.cs` — the token route deliberately carries **no membership filter**, for the same reason the attachment routes do not: it names only a meeting, so applying the filter would mean trusting a caller-supplied conversation id. Two *distinct* 503 problem types — 'the media host is down' and 'the platform is busy' call for different responses from both a person and a dashboard — and both say messaging is unaffected in the title
- [X] T191 [US8] Emit `MeetingStarted` and `MeetingEnded` over SignalR in `src/InternalChat.Api/Consumers/MeetingLifecycleConsumer.cs` — sent to the conversation's group, which is already maintained by `MembershipChangeConsumer`, so someone removed from the conversation stops receiving join prompts without this consumer knowing anything about membership. `MeetingEnded` earns its place because **nothing a client can observe locally tells it the prompt is stale**: a meeting ends when the room empties, possibly long after the reader stopped looking, and without it a conversation would offer to join a call that finished an hour ago
- [X] T192 [US8] Emit audit events for meeting start, join, leave, and end in `src/InternalChat.Worker/Consumers/MeetingAuditConsumer.cs` — on a **separate queue** from `meetings.lifecycle` despite the identical binding: one queue consumed by two hosts hands each message to whichever gets there first, so half the meetings would be announced and the other half audited. Duration is recorded as a number rather than left to be derived, because the start record can be swept by retention before the end record is — making the subtraction impossible exactly when someone needs it. The end record has **no actor**: FR-047 means nobody ends a meeting, and naming the last person to leave would attribute a system event to someone who closed a tab
- [X] T193 [P] [US8] Build the lazy-loaded meetings feature so the LiveKit SDK stays out of the initial bundle in `src/internalchat-web/src/features/meetings/` — `index.ts` (the `React.lazy` boundary), `JoinPrompt.tsx`, and `useLiveMeeting.ts`. The join prompt is deliberately **not** in the lazy chunk: it renders for everyone in a conversation the moment a meeting starts, most of whom never join, so pulling the SDK down to show a button would defeat the split. Installed `livekit-client` + `@livekit/components-react`, and **`npm audit` then reported a HIGH advisory (js-yaml)** — the constitution blocks merge on High with a fix available, so `npm audit fix` was applied; 3 moderate dev-only advisories remain
- [X] T194 [P] [US8] Build the join prompt, participant tiles, and mute and camera controls in `src/internalchat-web/src/features/meetings/MeetingRoom.tsx` — **FR-048's 'MUST NOT reveal any other application' is enforced by the browser's own picker, not by this code**, and that is stated in the file because it otherwise looks like an omission: a page cannot enumerate windows, and a single-window capture never receives frames from anything else, so the platform is the strongest possible place for that guarantee. `RoomAudioRenderer` is present deliberately — without it participants see each other and hear nothing, a failure that looks like a network problem and is a missing element. Lint caught two real defects: reading `Date.now()` during render (now a timer effect) and mirroring `isScreenShareEnabled` into redundant state
- [X] T195 [US8] Add a bundle-size assertion confirming meetings are excluded from the initial chunk in `src/internalchat-web/vite.config.ts` — a build-time plugin that walks the entry chunk's **static** import graph, sums gzipped bytes against plan.md's 300 KB budget, and fails if `livekit-client` or `@livekit/components-react` appears in it. **Verified by deliberately breaking it**: adding a static `import { MeetingRoom }` to `App.tsx` failed the build naming the offending module; reverting restored it. Current initial bundle: **109.7 KB of 300 KB**. A build-time gate rather than a CI script on purpose — a size regression that only fails in CI lands on a branch and is found by somebody else
- [X] T196 [US8] k6 load test to 25 participants per room and the 1,250 platform ceiling in `tests/Load/meetings.js` — written and syntax-checked; **not executed** (needs the seeded stack and a running media host). The file states plainly what it can and cannot establish: it measures the **API's** refusal behaviour at both ceilings (409 at 25, 503 with `retryAfterSeconds` at the platform limit), and it does **not** establish that the media host carries 1,250 participants — k6 mints tokens, opens no WebRTC transports and consumes no SFU CPU. `setup()` **fails** rather than warns when the media host is unreachable, because otherwise every start would be refused with 503 and the run would report a flawless pass having measured the degradation path instead of the ceiling
- [ ] T197 [US8] Measure actual media host capacity on real hardware — the research D12 open item — and record it in `specs/001-enterprise-chat-platform/research.md` — **BLOCKED, and left unchecked deliberately.** There is no media host to measure: this needs real machines and a WebRTC load generator that publishes tracks, neither of which exists in this environment. What *was* done is record the open item properly in research.md under D12 — that 1,250 is a **design figure** (50 × 25) derived from the requirement rather than from any observation, that neither T196 nor T198 can establish it, and a table of what each possible measurement would change. The ceiling is configurable (`MeetingCapacityOptions.MaximumConcurrentParticipants`) precisely so the measured number replaces it without a code deployment. The measurement's real deliverable is knowing **which resource runs out first**, because that decides whether the next host is bigger or there are two
- [X] T198 [US8] Playwright E2E covering quickstart V7 meeting scenarios in `tests/e2e/v8-meetings.spec.ts` — 6 tests, written and type-checked, **not executed**. Two gaps recorded rather than one: the usual unbuilt Compose images, *and* that anything involving real media needs a running LiveKit. Media-dependent tests are **gated on `MEDIA_HOST=true`** rather than skipped silently, so a run without one reports what it did not cover. One test runs **without** a media host and is the only one of its kind in the project: it asserts that a real page load never fetches the meetings chunk — the Vite plugin checks the build output, this checks what the browser actually downloads

**Checkpoint**: Meetings work without affecting messaging.

---

## Phase 11: User Story 9 - Screen Sharing in Meetings (Priority: P4)

**Goal**: A participant shares a screen or a single window legibly, with one defined rule when two
people share at once, and no leakage of other applications.

**Independent Test**: Share a single window, switch applications, and confirm the other application
is not revealed; start a second share and confirm the defined rule applies visibly.

### Tests for User Story 9 (REQUIRED) ⚠️

- [X] T199 [P] [US9] Unit tests for the single-publisher screen-share rule in `tests/Unit/Domain/ShareSessionTests.cs` — 13 tests. Unusual in that they **pin down a choice rather than derive one**: FR-050 asks only for 'one defined, visible rule', and the rule chosen is last-writer-wins with the displaced sharer told why. Refusing the second sharer reads as more polite and behaves worse in the case that actually happens — someone is presenting, the meeting moves on, and the next person cannot take over until the first notices. The stop *reason* is what makes the rule visible; without it a client can only say 'sharing stopped'
- [X] T200 [P] [US9] Integration test asserting a second simultaneous share request is handled by the defined rule rather than left ambiguous in `tests/Integration/Meetings/ConcurrentShareTests.cs` — 10 tests. The concurrency case is why this needs a real database: the handler stops the current share before starting the next, which is correct sequentially and racy under load, and what makes it safe is a **partial unique index in PostgreSQL** that no unit test could assert. Also covers the superseded participant's stop being *accepted* rather than an error — their client still sends one when it closes the picker, and a 404 there would surface a failure nobody caused

### Implementation for User Story 9

- [X] T201 [US9] Enforce the single screen-share publisher server-side in `src/InternalChat.Application/Meetings/StartScreenShare.cs` — plus `IShareSessionStore`, its EF mapping, the store, the `ScreenShare` migration and the two endpoints. **Server-side because the browser will happily let two people publish a screen-share track at once and LiveKit will forward both**, producing two shared screens — neither of the outcomes FR-050 permits. Membership is re-checked on every claim rather than trusted from the join token, which was minted up to five minutes ago and cannot be revoked (FR-030). **Verified against real PostgreSQL, 7 cases**: a second active share refused by `ux_share_session_one_active_per_meeting`, a new one permitted once the first stops, many *stopped* shares coexisting (the index is partial), and a stop without a reason refused
- [X] T202 [US9] Track `shared_screen_seconds` per participation — implemented in `ApplyMediaSignal` rather than the file this task names, because T191 correctly put `MeetingLifecycleConsumer` in the **API** (only a process holding a WebSocket can announce over SignalR), so the Worker path this task assumed does not exist. The totals are **set from the summed share sessions, not accumulated per event**: at-least-once delivery would otherwise make the recorded figure depend on how many times a webhook was redelivered. An open share is stopped *before* the totals are summed, since a session with no stop time has no duration and the final share would be dropped from the FR-051 figure entirely. A participant who leaves also releases the slot — otherwise it stays claimed by someone absent and nobody else can take it
- [X] T203 [P] [US9] Build the share control with a full-screen versus single-window picker in `src/internalchat-web/src/features/meetings/ScreenShareControl.tsx` — **the scope choice happens in the browser's own picker, not in this UI, and that is the strongest possible arrangement**: FR-048's 'MUST NOT reveal any other application' is a guarantee a web page cannot make about itself (it cannot enumerate windows, and a single-window capture never receives frames from outside it). Capture is started *before* the slot is claimed, because the picker can be cancelled and claiming first would displace the current sharer on behalf of someone who then changed their mind
- [X] T204 [P] [US9] Build the enlarge-shared-view control and the "you are viewing X's share" indicator in `src/internalchat-web/src/features/meetings/SharedView.tsx` — the indicator is **FR-050's visible rule from the viewer's side**: when sharing changes hands the picture changes and nothing else does, so without a name on it a viewer cannot tell a handover from the presenter switching applications. Enlarging is FR-049, which is partly a media question and partly a layout one — a 1080p document tiled beside five faces in a narrow column is unreadable however good the stream is
- [X] T205 [US9] Playwright E2E covering quickstart V7 screen-share scenarios in `tests/e2e/v9-screenshare.spec.ts` — 5 tests, **written and type-checked, not executed** (unbuilt images, and media tests gated on `MEDIA_HOST=true`). **One scenario is deliberately not automated**: quickstart V7 step 4 asks the tester to share one window, switch applications, and confirm the other is not revealed. No browser automation can verify that — Playwright cannot drive the OS picker, cannot alt-tab between real applications, and cannot see what a capture would have contained. Faking it would assert that Chromium reports what we asked for, not that the guarantee holds. It is enforced by the platform and verified by a human in T219; recorded in the file rather than left as an apparent coverage gap

**Checkpoint**: All nine user stories are independently functional.

---

## Phase 12: Polish, Retention & Cross-Cutting Concerns

**Purpose**: Operational obligations and platform-wide verification

> **Retention is not optional polish.** FR-052 through FR-055 are compliance requirements and the
> sweep must exist and be verified before first production use, even though its effect is not
> visible for 12 months. It sits here only because it depends on every entity existing first.

- [X] T206 Implement the retention sweep using partition drops with per-batch audit events in `src/InternalChat.Worker/Jobs/RetentionSweepJob.cs` — plus `IRetentionSweep` and its PostgreSQL implementation. Messages go by `DROP TABLE` on a whole partition, which is the entire reason `message` is partitioned: at 125 M rows a monthly `DELETE` would rewrite tens of millions of rows, bloat the table and hold locks for hours. Attachments cannot work that way — the table is not partitioned and each row owns a MinIO object — so they go in bounded batches, **object first, row second**, since the reverse order loses the key that identifies the object and creates an orphan nothing can find. **Daily rather than monthly**: a monthly schedule has one chance to fire and a Worker restarting then misses it silently. An audit record is written on **every** pass including the ones that delete nothing, because silence is indistinguishable from the job being broken. Uses `IOptionsMonitor` so FR-054's administrator change takes effect without a restart
- [X] T207 Implement the retention policy endpoints with audited changes in `src/InternalChat.Api/Endpoints/AdminEndpoints.cs`
- [X] T208 [P] Display the active retention period to every employee in `src/internalchat-web/src/features/settings/RetentionNotice.tsx`
- [X] T209 Implement the export job with audit recording in `src/InternalChat.Worker/Jobs/ExportJob.cs`
- [X] T210 Implement the orphaned object reclamation sweep in `src/InternalChat.Worker/Jobs/OrphanReclaimJob.cs`
- [X] T211 Integration test asserting the retention tolerance in `tests/Integration/Retention/RetentionToleranceTests.cs` — 8 tests. **Asserts both directions, and the second matters more**: that an entirely-expired partition really is dropped (its rows vanish because the table holding them does — no `DELETE` runs), and that a partition *straddling* the cutoff is **kept**. Dropping the straddler would take messages still inside the retention window, which is data loss rather than retention — the first failing is a compliance gap, the second failing is deleting people's messages early. The bound parser is tested directly because a misparse either spares everything forever or deletes a month early, and **both look like the sweep working**. Also asserts the sweep is idempotent, since it runs daily and the second pass must not error on a table that is already gone — nothing deleted early, nothing surviving more than 7 days past — in `tests/Integration/Retention/RetentionToleranceTests.cs`
- [X] T212 Create scripted PostgreSQL and MinIO backup with a rehearsed restore in `deploy/scripts/backup.sh` and `deploy/scripts/restore.sh`
- [X] T213 Integration test asserting a full cache-tier outage causes latency only, never data loss or an incorrect access decision, in `tests/Integration/Resilience/CacheOutageTests.cs`
- [X] T214 [P] Security verification pass over authorization policies, rate limits, headers, CSP, and audit coverage in `docs/security-review.md`
- [X] T215 [P] Accessibility pass to WCAG 2.1 AA on interactive components in `src/internalchat-web/tests/a11y.test.ts`
- [X] T216 [P] Configure the Lighthouse CI bundle and Core Web Vitals budget in `.github/workflows/ci.yml`
- [X] T217 Verify all per-layer coverage floors are met and enforced in `.github/workflows/ci.yml` — **all four now pass**: Domain 96.32% (floor 90), Application 93.49% (85), Infrastructure 85.66% (60), React 98.32% (80). Three things had to happen before that number meant anything.

  **The gate itself was under-reporting.** It took the highest line-rate any single Cobertura report observed. A layer exercised partly by the unit suite and partly by the integration suite has neither report showing the whole picture, and the maximum of two partial views is still partial. Replaced with a true union of covered lines, keyed by file and line rather than by class — a partial class (every `LoggerMessage`-generated type here) appears in more than one report under names that do not always agree. The fix moved the numbers *down* first, which is how it was confirmed to be a fix rather than a flatter.

  **Domain and Application were genuinely below the floor** (79.33% and 72.53%). Closed with `DomainExceptionTests`, `IdentityAndEqualityTests`, `ValidatorTests`, `SecurityAuditorTests`, `ApplicationExceptionTests` and `ApplyMediaSignalTests` — 92 new unit tests. The exception types are tested rather than excluded because their carried state (`LimitBytes`, `Capacity`, `Current`) is what `ProblemDetailsHandler` turns into a response someone acts on; a constructor that dropped one would produce a technically-correct 413 that tells the sender nothing.

  **The React floor had never been measured at all** — `vitest.config.ts` declared no coverage provider, so the gate printed SKIP and CI's `-Frontend` switch was the only thing that would have caught it. Configured with `all: true`, without which deleting the last test for a module *raises* the percentage. First measurement was **14.57%**: the six existing test files covered pure library modules and nothing else — no component, no API client, no service worker. Closed with 19 new test files and 350 new tests.

- [ ] T218 Run the full messaging-path load test against seeded production-volume data in `tests/Load/`
- [ ] T219 Execute the complete quickstart validation V1 through V9 in `specs/001-enterprise-chat-platform/quickstart.md`
- [ ] T220 [P] Write the operations runbook covering deployment, rollback, backup, and DLQ handling in `docs/runbook.md`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately
- **Foundational (Phase 2)**: Depends on Setup — **BLOCKS all user stories**
- **US1 (Phase 3)**: Depends on Foundational. Blocks every other story, because nothing can be
  authorized until the authorization pipeline exists.
- **US2 (Phase 4)**: Depends on US1
- **US3 (Phase 5)**: Depends on US2 — reuses its delivery mechanics
- **US4 (Phase 6)**: Depends on US2; most valuable after US3
- **US5 (Phase 7)**: Depends on US2
- **US6 (Phase 8)**: Depends on US2; needs US3 for meaningful membership scoping
- **US7 (Phase 9)**: Depends on US5 — extends the same attachment pipeline
- **US8 (Phase 10)**: Depends on US2. **Additionally gated** on the application host meeting its
  messaging budgets.
- **US9 (Phase 11)**: Depends on US8 — meaningless without it
- **Polish (Phase 12)**: Depends on all entities existing

### Genuine parallelism after Phase 4

Once US2 is complete, three tracks can run concurrently with different people:

| Track | Stories | Overlap risk |
| --- | --- | --- |
| A | US3 → US6 | Touches conversation and search paths |
| B | US4 | Touches notification and read-state paths only |
| C | US5 → US7 | Touches attachment and storage paths only |

US8 and US9 are a fourth track but should not start until Track A, B, or C has proven the
messaging budgets hold under load.

### Within Each User Story

- Tests MUST be written and observed FAILING before implementation (Principle III)
- Domain before Application; Application before Infrastructure; Infrastructure before Presentation
- Story complete before moving to the next priority

---

## Parallel Example: User Story 2

```bash
# Launch all tests for User Story 2 together (write them FIRST, confirm they fail):
Task: "Unit tests for message body length and edit window in tests/Unit/Domain/MessageTests.cs"
Task: "Unit tests for server-sequence ordering in tests/Unit/Domain/MessageOrderingTests.cs"
Task: "Contract tests for message endpoints in tests/Contract/MessagingContractTests.cs"
Task: "Integration test for duplicate clientMessageKey in tests/Integration/Messages/IdempotentSendTests.cs"

# Launch both domain entities together:
Task: "Create Message aggregate in src/InternalChat.Domain/Messages/Message.cs"
Task: "Create Conversation aggregate in src/InternalChat.Domain/Conversations/Conversation.cs"

# Launch all frontend work together once the API contract is stable:
Task: "Conversation list in src/internalchat-web/src/features/conversations/"
Task: "Virtualized message list in src/internalchat-web/src/features/messages/MessageList.tsx"
Task: "Composer with offline queue in src/internalchat-web/src/features/messages/Composer.tsx"
Task: "SignalR client with Resync in src/internalchat-web/src/lib/realtime/"
```

---

## Implementation Strategy

### MVP First (US1 + US2)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational — **critical, blocks everything**
3. Complete Phase 3: US1 — authentication and authorization
4. Complete Phase 4: US2 — direct messaging
5. **STOP and VALIDATE**: run quickstart V1 and V2, and measure the performance budget against
   seeded full-volume data
6. Deploy and demonstrate

At this point 10,000 employees can sign in and message each other reliably. Everything after is
additive.

### Incremental Delivery

1. Setup + Foundational → foundation ready
2. US1 + US2 → **MVP**, deployable
3. US3 → team conversation
4. US4 → the platform becomes a reliable channel
5. US5 → screenshots, the most common attachment
6. US6 → history becomes searchable
7. US7 → video files
8. US8 + US9 → meetings, requiring the second host

Each increment is independently deployable and does not break the previous one.

### Constitution Gates at Every Increment

No increment is done until: tests were written first and pass; coverage floors hold; the declared
performance budget is measured and met; security controls for the touched surface are verified;
structured logs, metrics, and traces cover new paths; migrations apply forward cleanly; and the
OpenAPI document and message contracts are updated.

---

## Notes

- [P] tasks touch different files and have no incomplete dependencies
- [Story] labels map tasks to spec.md user stories for traceability
- Verify tests fail before implementing — NON-NEGOTIABLE per Constitution Principle III
- Every new endpoint, hub method, and consumer needs an explicit authorization policy task; the
  architecture test at T019 fails the build if one is missing
- Every cache write task needs a matching invalidation task and integration test
- Every RabbitMQ consumer task must state its idempotency key
- Commit after each task or logical group
- Stop at any checkpoint to validate a story independently
